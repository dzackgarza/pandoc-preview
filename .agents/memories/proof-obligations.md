# Proof Obligations (P1–P119)

User-approved external proof obligations for Pandoc Preview.
Each is an exact, externally observable happy-path state — real display, real pandoc, real filesystem, real XDG config.
No internal behaviours, no forced error modes.
An assertion is admissible only if it would fail on a plausibly broken app (unwired pandoc, frozen preview, UI-only fake state, junk output).

P45–P50 (added 2026-06-16) cover the Tier 1 "Recovery and Git State" milestone, authored from [[recovery-and-git-state-requirements]]. Recovery durability is the deepest priority in the app: the core guarantee is that no more than several seconds of work is ever permanently lost, recovery lives on the HOST FILESYSTEM (never browser storage), and corruption is treated as strictly worse than a crash.

## Shared witness fixture

A per-run temp project containing `demo.md` with:

- heading `# Geometry of Numbers — Café` (unicode discriminator)
- `*naïve*` emphasis
- an ordered list whose last item is `Minkowski bound`
- math line `$\zeta(2) = \pi^2/6$`
- `![scatter](fig/plot.png)` referencing a real 64×48 PNG at `fig/plot.png`

## Obligations

- **P1 — Source→preview fidelity.** Open project, click `demo.md`. Preview iframe document contains `h1` with text exactly "Geometry of Numbers — Café", an `em` with text "naïve", an `ol` whose last item is "Minkowski bound".
- **P2 — Live re-render.** Type `The discriminant equals −163.` at buffer end.
  Sentence absent from preview before the edit; present verbatim after the configured debounce elapses.
- **P3 — Save persists exact bytes.** After the P2 edit, Save via the File menu surface (File > Save / Ctrl+S — Save is NOT a toolbar button).
  Independent process reads the file from disk: byte-for-byte equal to the editor buffer, unicode intact.
- **P4 — Math rendering (MathJax, always).** Math is hard-coded to MathJax — KaTeX cannot cover pandoc's full math syntax range, so no engine option exists anywhere (config, settings UI, first-run script).
  Preview contains `span.math mjx-container` whose assistive MathML flattens to exactly `ζ(2)=π2/6`; the literal `$\zeta(2)` does not appear as text.
- **P5 — Relative resource resolution.** Preview `img[alt="scatter"]` has `naturalWidth == 64 && naturalHeight == 48` (real pixels decoded through the asset-protocol `<base href>` chain).
- **P6 — File manager mutates the real directory.** (a) Sidebar lists exactly the non-hidden entries of the opened folder, directories first.
  (b) Creating `chapter2.md` via the UI yields a real empty file on disk and the editor opens it.
  (c) Renaming to `chapter-two.md` makes the old path absent and the new present on disk.
- **P7 — Export HTML artifact (shipped default plugin).** Export HTML to a chosen temp path: file exists at exactly that path, parsed DOM repeats P1 witnesses, `img` `src` is a `data:` URI (self-contained).
  The export runs the configured `[export.html]` plugin command ([[export-plugins-contract]]), not a hard-coded invocation.
- **P8 — Export PDF artifact (shipped default plugin).** Export PDF via the configured `[export.pdf]` plugin: valid PDF whose extracted text contains "Geometry of Numbers" and "Minkowski bound", AND whose `pdfinfo` Creator/Producer discriminates the configured engine (lualatex → LuaTeX, not pdfTeX) — proving the configured command is what actually ran.
  lualatex is a hard dependency — fail loudly, never skip.
  (Revised 2026-06-13: the original P8 never discriminated the engine; exports silently ran pandoc's implicit pdflatex default.)
- **P9 — Settings round-trip to XDG TOML.** With hermetic `XDG_CONFIG_HOME`, change font size 14→18 and theme dark→light via Settings, save.
  On-disk `pandoc-preview/config.toml` parses to exactly `font_size = 18`, `theme = "light"`, all other keys unchanged; editor computed font-size 18px.
- **P10 — First-run script → bootable app.** Drive `scripts/first-run.sh` in a real PTY through the gum prompts: config.toml parses to exactly the selected values, and a subsequent app launch reaches the editor UI (not the config-error screen).
- **P11 — Compile log reflects the real subprocess.** After a successful render, the Compile Log tab contains the configured `--from markdown` and a zero exit status.
  (Known cosmetic defect found at design time: `render.rs::format_log` produces `exit status: exit status: 0`.)
- **P12 — Custom export pipeline honored.** A user-defined `[export.<id>]` plugin whose command is an arbitrary executable (e.g. a script writing a discriminating witness derived from the real input file to `{output}`) runs verbatim on export: the witness file appears at exactly the chosen path with content proving the configured argv ran against the real source.
  Proves the export surface is plugin-shaped, not pandoc-shaped ([[export-plugins-contract]]).
- **P13 — Splitter tracks the pointer.** Dragging the editor/preview divider moves it to the pointer position (within a few px), including when the drag path crosses the preview iframe, and it never sticks or jumps.
  (Observed bug 2026-06-13: hand-rolled drag in App.svelte — no pointer capture, so the iframe swallows pointermove; ratio computed against the whole main row including the sidebar, so the divider does not land at the pointer.)
- **P14 — Tab switch preserves the split.** Switching Preview ↔ Compile Log changes neither pane's width.
  (Observed bug 2026-06-13: clicking the Compile Log tab makes pane sizes jump.)
- **P15 — Sidebar toggle preserves the editor:preview ratio.** Hiding/showing the file tree keeps the relative split of the two panes.
- **P16 — Preview math renders with no network (local MathJax).** With the webview's network blocked (no route to any CDN), open `demo.md`; the preview still satisfies P4 (`span.math mjx-container`, assistive MathML flattening to `ζ(2)=π2/6`). Discriminates a CDN-sourced MathJax — the current bare `--mathjax` preview leaves raw `$\zeta(2)` text offline.
  The MathJax `<script src>` resolves to the bundled app asset via the asset protocol, not `https://`. ([[mathjax-offline-local-source-decision]], decision A 2026-06-13.)
- **P17 — Exported HTML renders math offline (self-contained, no remote MathJax).** Export HTML via the shipped `[export.html]` plugin while offline: export exits 0 with no "Could not fetch" warning, the artifact contains NO `https://`/`cdn.jsdelivr` reference, and opening it in a network-blocked webview typesets the math (P4-shape `mjx-container`). Proves the shipped default inlines a local MathJax bundle (`--mathjax=<local>` + `--embed-resources`), not a CDN link.
  Strengthens P7 (which only asserted `data:` image URIs).
  ([[mathjax-offline-local-source-decision]].)
- **P18 — VSCode-style activity bar + collapsible side bar.** The left of the window is an always-visible activity bar (a vertical strip of view controls, built to hold more views later) plus a collapsible side bar showing the ACTIVE view.
  One view now: Explorer (file tree), control `[data-view="explorer"]`; side bar `[data-pane="sidebar"]` showing that view's content.
  The Explorer control exists; the side bar starts open showing the Explorer view (the file tree, listing the open project's files); clicking the active view's control collapses the side bar while the activity bar PERSISTS (the control stays, VSCode-style); clicking again reopens it.
  (Added 2026-06-13, revised same day from a single ☰ toolbar toggle to the VSCode activity-bar model so more view tabs can be added later.
  Revised 2026-06-16 (commit 1dbc698): the side bar no longer carries a separate "Explorer" label header — that was redundant with the file tree's own folder-name header, which now sits at the top of the side bar and names the active view.
  The obligation is that the side bar shows the active view's content, not that it carries a distinct view-label header.
  Save is a File menu / Ctrl+S item, not a toolbar button — see P3.)

### Tier 1 — Recovery and Git State (P45–P50)

- **P45 — Recovery captures unsaved edits durably.** While the user edits, the buffer is continuously captured to an app-owned recovery store ON THE HOST FILESYSTEM within several seconds, with no user action.
  Append a unicode-discriminating sentence (e.g. `Café résumé — naïve ζ.`) to the buffer WITHOUT saving; within several seconds, an independent process reading the host-filesystem recovery store finds a copy byte-equal to the current buffer (the appended sentence included, unicode intact), AND the project file on disk is still byte-for-byte unchanged.
  Admissible because it fails on a no-op autosave (recovery store never contains the sentence), an autosave that only uses browser storage (no host-filesystem copy exists for the independent reader), an autosave that fires only on Save (the project-file-unchanged clause forces a pre-save witness — recovery must precede any disk write), and a debounce too long to capture within several seconds.

- **P46 — Repo-state machine reflects and mutates real git state.** The app continuously reflects whether the open file is noRepo / untracked / tracked via a prominent indicator, with one-click shortcuts OUT of every degraded state.
  Open a file in a directory that is not a git repository: the indicator reads noRepo.
  Invoke "initialize repository": a real repository appears on disk (an independent git query against the directory succeeds) and the indicator becomes untracked.
  Invoke "start tracking": an independent git query reports the file as tracked, and the indicator becomes tracked.
  Admissible because it fails on a UI-only indicator that never reads real git (the independent git query contradicts the displayed state), a state hardcoded to one value (it never transitions across the three observed states), and a "track" action that relabels the indicator without actually staging the file (the independent git query still reports it untracked).

- **P47 — Path-consuming actions are gated on durable identity.** Save-in-place, export, and plugin-run on an identity-less (recovery-backed) buffer first resolve a real durable destination; until that destination is resolved, the action does not run.
  With a buffer that has no real file identity, invoke export: NO artifact is produced and the downstream command does NOT run.
  After a real destination is resolved, the file exists at exactly that destination, that destination becomes the live editable file going forward, and a later edit followed by Save writes to that same destination (independent disk read confirms).
  A buffer that already has a durable identity saves with no prompt at all.
  Admissible because it fails on an export that silently runs against the volatile buffer (an artifact appears with no destination resolved), a gate that auto-guesses a filename (a destination materializes that the user never chose), a gate that runs the downstream command anyway (the command's effect is observed despite no resolved destination), and a gate that re-prompts on an already-durable save (the no-prompt clause fails).

- **P48 — Save refuses to clobber an externally modified file.** Before writing, the app compares a fingerprint captured at open/last-save against the current on-disk state; if the file changed underneath the editor, Save is refused LOUDLY and the external content is preserved.
  Open a file; an independent process then rewrites it on disk with different content; the next in-app Save is refused with a VISIBLE error, the on-disk content remains exactly what the independent process wrote (the editor buffer did not win), and the buffer stays dirty.
  An explicit overwrite/reload resolution offered after the refusal then succeeds — the refusal is a real gate, not a dead end.
  Admissible because it fails on a blind overwrite (the editor buffer clobbers the external content), a never-captured fingerprint (no comparison happens, so Save proceeds), and a silent refusal (Save does nothing with no visible error, leaving the user unaware their work was not written).

- **P49 — Launch restores the last session and offers newer recovery content.** On launch the app reopens the last file/project and window state from host-filesystem session state, and when the recovery store is ahead of the on-disk file, offers to restore that newer content.
  Edit a file (recovery captures the unsaved change), then relaunch the app against the same host session-state and data locations: the editor reopens that file, a restore offer presents the newer recovery content, and accepting it loads the buffer including the unsaved edit (byte-equal to the recovery copy).
  Admissible because it fails on a launch that opens blank or a hardcoded file (the last file is not reopened), a restore offer sourced from browser storage (no host-filesystem session state drives the reopen), and a restore that loads the stale on-disk content instead of the newer recovery buffer (the accepted buffer lacks the unsaved edit).

- **P50 — Closing with a dirty buffer is guarded and loses nothing.** Closing the app (or switching files) with a dirty buffer prompts the user to resolve; the app does not close until the prompt is resolved; and because recovery already captured the buffer, no content is lost even on discard.
  With a dirty buffer, request close: a resolution prompt fires and the app stays alive (it does not close out from under the unsaved work).
  Independently, the host-filesystem recovery store holds the dirty content — the lose-nothing backstop that survives even a forced quit.
  Admissible because it fails on a close that drops the dirty buffer with no prompt and no recovery copy (work vanishes silently), and a prompt-only guard that still loses content on force-quit (the prompt blocks a graceful close but the recovery backstop is missing, so a hard kill loses the buffer).

### Tier 0 — Editor Completion (P51–P54)

P51–P54 (added 2026-06-16) cover the Tier 0 "Editor Completion" milestone: the editor's autocomplete is a composable surface that hosts multiple completion sources at once, a user-defined snippet dictionary is one such source that expands snippet bodies (not literal triggers) at the cursor, Emmet abbreviations expand into real markup at the cursor, and spellcheck marks misspelled words while honoring a custom math dictionary.

- **P51 — Composable editor completion.** The editor's autocomplete hosts MULTIPLE completion sources that COMPOSE — registering a new app completion source does not displace the completions that were already there.
  Register a new app completion source bound to a trigger, then drive the editor: typing that trigger opens the standard autocomplete tooltip and the newly-registered source's option appears in it, AND the pre-existing LaTeX completions still work in the same buffer — typing a backslash command (e.g. `\alpha`) offers the LaTeX completion, and typing the `:::` fenced-div trigger offers the fenced-div completion.
  Admissible because it fails on a wiring that lets one source monopolize or suppress the others (the new source's option appears but the LaTeX backslash/fenced-div completions no longer surface, or vice versa), and on a wiring that drops the LaTeX completions outright when a new source is added (the backslash and `:::` completions vanish once the new source is registered).

- **P52 — User-defined snippet dictionary expands.** A user-defined snippet dictionary, declared by a config-owned path (not a hardcoded list), surfaces as editor completions: typing a snippet's trigger opens the standard autocomplete tooltip offering a completion labeled by that trigger; accepting it expands the snippet BODY at the cursor — the inserted text is the snippet's expansion, not the literal trigger string, and the cursor lands at the snippet's declared tabstop.
  The dictionary path comes from config, so pointing config at a dictionary file makes those snippets the ones offered (a different dictionary offers different snippets).
  Admissible because it fails on a no-op source (the trigger is typed but never offered in the tooltip), a source that inserts the literal trigger text instead of the snippet body (accepting the completion leaves the trigger in the buffer rather than the expansion, and the cursor is not at the tabstop), and a dictionary that is ignored (config points at a dictionary whose triggers are typed but none of its snippets are ever offered).

- **P53 — Emmet abbreviation expands.** Typing an Emmet abbreviation in the editor and firing the Emmet-expand action replaces the abbreviation with the expanded markup at the cursor.
  Type an Emmet abbreviation that uniquely discriminates a real expansion (a multi-character/multi-element expansion Emmet produces, e.g. one that yields nested elements, repeated siblings, or attribute/class wrappers from a terse source) and invoke the Emmet-expand action: the abbreviation text is gone and the buffer at the cursor now holds the expanded markup it denotes, with the cursor landing inside the expansion.
  Admissible because it fails when the Emmet extension/keymap is absent — the abbreviation stays literal in the buffer (the expand action is a no-op and the typed abbreviation is echoed verbatim rather than replaced by the markup it expands to).

- **P54 — Spellcheck marks misspelled words, honoring a custom math dictionary.** The editor marks misspelled words as spelling errors while respecting a user-owned custom math dictionary, so that mathematical terms are not flagged.
  Type a clearly-misspelled token (random gibberish) into the buffer: it is visibly marked as a spelling error.
  Type an ordinary correctly-spelled English word: it is NOT marked.
  Type a mathematical term that ordinary English spellcheck would flag but which is present in the user's custom math dictionary: it is NOT marked, proving the custom dictionary is in effect.
  Admissible because it fails when there is no spellcheck (nothing is marked, so the gibberish token is not flagged), on a checker that marks everything (the correctly-spelled English word and the dictionary math term are both wrongly flagged), on a checker that marks nothing (the gibberish token is never flagged), and on a checker WITHOUT the custom math dictionary (the math term is wrongly marked even though the dictionary lists it).

### Milestone G — Insertion Bar (P55–P62)

P55–P62 (added 2026-06-16) cover the Milestone G "Insertion Bar" milestone: the editor's top edit bar is a math-research INSERTION bar (not a generic formatting toolbar) that inserts amsthm environments, tikz/tikzcd diagram scaffolds, matrix environments of a chosen shape, pandoc pipe-tables of a chosen shape, config-declared snippets via a bar dropdown, language-tagged fenced code blocks via a dropdown, complete footnotes (marker plus definition) via a modal, and an image pasted from the system clipboard persisted as a real file in the configured global figures directory — all at the cursor.

- **P55 — Insertion bar replaces the formatting toolbar and inserts amsthm environments.** The editor's top edit bar is a math-research INSERTION bar; the generic H1/bold/italic formatting toolbar is gone.
  Selecting a named amsthm environment (e.g. `theorem`) from the bar inserts that environment's fenced-div scaffold (`:::{.theorem} … :::`) at the cursor, leaving the cursor at the environment body.
  Admissible because it fails if the old formatting toolbar is still present or its buttons insert markup like `**bold**` (the bar is still a formatting toolbar, not an insertion bar), on a no-op insert (selecting the environment leaves the buffer unchanged so no scaffold appears at the cursor), and if the wrong environment is inserted (selecting `theorem` inserts a fenced div whose class is not `theorem`).

- **P56 — tikz/tikzcd scaffold insert.** Selecting a tikz or tikzcd scaffold from the insertion bar inserts the corresponding diagram skeleton at the cursor, leaving the cursor inside the diagram body.
  Admissible because it fails on a no-op insert (selecting the scaffold leaves the buffer unchanged so no skeleton appears at the cursor), on a wrong-kind insert (a tikz skeleton is inserted when tikzcd was chosen, or a tikzcd skeleton when tikz was chosen), and when the cursor is not placed inside the diagram body after the insert.

- **P57 — Matrix builder.** Choosing matrix dimensions (rows×cols) on the insertion bar inserts a LaTeX matrix environment of exactly that shape at the cursor.
  Admissible because it fails on a no-op insert (choosing dimensions leaves the buffer unchanged so no matrix appears at the cursor), on a fixed-size insert that ignores the chosen dimensions (a different rows×cols matrix is inserted than the one chosen), and on a malformed matrix (the inserted environment has the wrong number of rows or columns for the chosen shape).

- **P58 — Table builder.** Choosing table dimensions (cols×body-rows) on the insertion bar inserts a pandoc pipe-table of exactly that shape at the cursor: a header row, an alignment separator row, and the chosen number of body rows, where every row carries the chosen number of `|`-delimited cells.
  Admissible because it fails on a no-op insert (choosing dimensions leaves the buffer unchanged so no table appears at the cursor), on a fixed-shape insert that ignores the chosen dimensions (a different cols×body-rows table is inserted than the one chosen), and on a missing or malformed separator row (without the alignment separator row the inserted text is not a valid pandoc pipe-table).

- **P59 — Snippet dropdown on the bar.** The insertion bar surfaces the config-declared snippet dictionary's triggers in a dropdown; choosing a trigger from that dropdown inserts the snippet's expanded BODY (not its literal trigger string) at the cursor, with the cursor landing at the snippet's declared tabstop.
  The dropdown's contents come from the config-declared snippet dictionary, so pointing config at a different dictionary makes the bar's dropdown surface that dictionary's triggers.
  Admissible because it fails on an empty or hardcoded dropdown that ignores the config dictionary (the dictionary's triggers never appear as dropdown entries), on literal-trigger insertion (choosing the entry leaves the trigger string in the buffer rather than the expanded body, and the cursor is not at the tabstop), and on an ignored dictionary (config points at a dictionary whose triggers are never surfaced in the dropdown).
  This is distinct from P52, which proves the autocomplete-popup path (typing a trigger in the buffer opens the completion tooltip); P59 proves the BAR-dropdown path (selecting a trigger from the insertion bar's dropdown).

- **P60 — Code-block-type dropdown.** Choosing a language from the insertion bar's code-block-type dropdown inserts a fenced code block tagged with that language at the cursor: an opening fence carrying the chosen language tag (`` ```<lang> ``) and a matching closing fence, with the cursor placed inside the block.
  Admissible because it fails on a no-op insert (choosing a language leaves the buffer unchanged so no fenced block appears at the cursor), on an untagged block that ignores the chosen language (the opening fence carries no language tag), and on a wrong language tag (the inserted fence is tagged with a language other than the one chosen).

- **P61 — Footnote modal.** A footnote action on the insertion bar opens a modal in which the user types the footnote body; on confirm, a COMPLETE footnote is inserted — a reference marker (`[^id]`) at the cursor AND a footnote definition line (`[^id]: <body>`) whose body is exactly the text the user typed.
  Admissible because it fails on a no-op insert (confirming the modal leaves the buffer unchanged so neither marker nor definition appears), on a marker-only insert with no definition (the reference marker is placed but the typed body is lost because no `[^id]:` definition line is inserted), on a plain-text insert (the typed body lands in the buffer as ordinary text rather than as a footnote marker-plus-definition pair), and on a body mismatch (the inserted definition's body is not byte-equal to what the user typed in the modal).

- **P62 — Insert image from clipboard.** With an image on the system clipboard, the insertion bar's paste-image action writes the image as a real file into the CONFIGURED GLOBAL figures directory and inserts a markdown image reference (`![…](…)`) to that exact file at the cursor.
  An independent process reading the configured global figures directory finds a newly-written image file whose bytes are the clipboard image (real image bytes are persisted, not zero-length), and the markdown reference inserted at the cursor points at that same on-disk file.
  Admissible because it fails on a no-op insert (the paste-image action leaves the buffer unchanged so no image reference appears at the cursor), on a dangling reference (a markdown image reference is inserted but it points at a file that was never written / does not exist on disk), on a wrong-location write (the file is written into a local `./figures` relative to the project instead of the configured global figures directory), and on an empty persist (the reference points at a file that exists but holds no image bytes — nothing of the clipboard image was actually persisted).

### Milestone — Export as a discovered plugin (P66)

P66 (added 2026-06-17) covers the milestone that retires the transitional `[export.<id>]` app-config tables ([[export-plugins-contract]]) by making export a discovered plugin in the pandoc suite — exactly as rendering already moved to the pandoc-renderer plugin.
The app core owns NO pandoc/export command knowledge; the export flags live inside the export plugins themselves.

- **P66 — Export is a discovered plugin in the pandoc suite.** A plugin declaring `category = "export"` placed in the plugins directory is discovered the same way the pandoc-renderer plugin is — no app-core `[export.<id>]` config table is involved anywhere.
  Once discovered, that plugin auto-populates an "Export: <name>" menu / command-palette entry that carries the plugin's declared output extension (a generic manifest field on the export-category plugin, read by the menu populator), AND the plugin contributes its own row to the doctor check.
  Admissible because it fails if the export menu still reads the app-core `[export.<id>]` config (an export plugin that declares NO `[export.*]` table would then never appear in the menu), if discovery ignores `category` (a non-export plugin populates an "Export:" entry, or the export plugin does not), and if the export plugin contributes no doctor row (the doctor output has no entry attributable to that discovered export plugin).

## Export-as-plugin migration rulings (2026-06-17)

Ratified rulings for the export-as-plugin-suite migration.
Apply these; do not re-litigate.

1. **MathJax (total externality).** The HTML export plugin VENDORS its own MathJax bundle INSIDE its plugin dir (copy from `src-tauri/resources/mathjax/tex-full-svg-a11y.min.js`) and references it locally — NOT via an AppHandle resource path.
2. **Independent raw command per plugin (richness bar).** Each export plugin carries its OWN independent raw command; export commands are individually managed per plugin.
3. **Enable/disable OUT OF SCOPE this milestone.** Export plugins are discovered + run + doctored + configured exactly like the pandoc-renderer plugin — no enable/disable.
4. **Output extension is a generic manifest field.** The output file extension is a generic manifest field on the export-category plugin, read by the menu populator.

These map to the next spec families the obligations document already tracks: webview specs p45–p50 and doctor-class specs d17+. The spec design itself belongs to the test author and implementer, not to this obligations document.

### Milestone — Phase A: lint fast-feedback (P70–P74; P75 HELD, P76 reserved)

P70–P74 (added 2026-06-18) cover the Phase A "lint fast-feedback" milestone, authored from [[phase-a-lint-fast-feedback]]. The deliberate P67–P69 gap is intentional: the live contract above runs P1–P66, and the phase obligations were shifted to P70+ rather than reusing 67–69. Each obligation is an exact externally-observable happy-path state, driven by the real app via the `tauri-plugin-playwright` harness against the shared witness fixture, observing real CM6 `@codemirror/lint` diagnostics and real config.
Each is admissible only if it FAILS on a plausibly broken app.

- **P70 — Static delimiter & math-mode balance warns before compile.** With a buffer containing a real imbalance — e.g. a math line with two `\left` and one `\right`, and a separate `$`-opened math zone with no closing `$` — the editor surfaces, via the `@codemirror/lint` diagnostics field (observed through the `lintDiagnostics()` hook), at least one diagnostic whose marked range covers the surplus `\left`/the unterminated `$` and whose message names the imbalance; AND after the user balances them (append the missing `\right` and `$`), that diagnostic is gone.
  NO latex COMPILE / preview RENDER is the source — the `\left`/`\right` half comes from the cheap real-ChkTeX lint pass on the emitted `.tex` (pandoc `md→tex` + `chktex`), while the math-mode `$`-balance half is markdown-native, computed by the `pandoc-md-lint` tool directly on the buffer because pandoc escapes a lone markdown `$` to `\$` so chktex's warning 16 never fires on the `.tex`; either way it is the cheap lint pass, not the full pandoc-HTML render, which is exactly "feedback faster than a compile."
  Admissible because it fails on: no app lint source (the diagnostics field has only the fork's `\begin`/`\end` checks, the `\left`/`$` imbalance is never marked); a cursor-pair-highlighting-only implementation (highlighting shows the pair under the cursor, never a buffer-wide COUNT imbalance, so no diagnostic exists); and a stuck linter (the diagnostic persists after the user balances).

- **P71 — Typographic lint fires only when its class is enabled, scoped to math.** With `[editor.lint]` enabling the operator-as-variable and sub/superscript-grouping classes, a math line containing `sin x` and `x^10` yields diagnostics marking `sin` (→ `\sin`) and `x^10` (→ `x^{10}`); the SAME tokens in PROSE (outside `$…$`) yield none; and with those classes disabled in config, the math-line diagnostics are absent.
  Admissible because it fails on: a missing typographic layer (the math `sin`/`x^10` are never marked); a layer that ignores math scope (prose `sin` is wrongly marked); and a layer that ignores config (disabling the class in config still marks the tokens — the config is dead).

- **P72 — User-defined regex lint rule surfaces as a diagnostic.** With a config-owned `lint_rules` entry `{pattern: "\\bTODO\\b", message: "resolve before submission"}`, typing `TODO` into the buffer yields a diagnostic whose range covers `TODO` and whose message is exactly `resolve before submission`; pointing config at a DIFFERENT rule set (a different pattern/message) makes the buffer surface THAT rule's diagnostic instead.
  Admissible because it fails on: no user-regex engine (the config rule is ignored, `TODO` is never marked); a hardcoded rule list (a different config rule set produces the same diagnostics, proving config is not read); and a message mismatch (the diagnostic's message is not the config-declared string).

- **P73 — In-document suppression silences a named lint on its line.** With a buffer line that triggers a known lint (e.g. an intentional `x^10` in math), adding the directive comment `<!-- ppe-lint-disable-line <ruleId> -->` for that line's rule removes the diagnostic on THAT line while the SAME construct on another (un-suppressed) line still warns; removing the directive restores the diagnostic.
  Admissible because it fails on: no suppression scanner (the directive is inert, the diagnostic persists); an over-broad suppression (the directive on one line silences the construct everywhere); and a suppression that never restores (removing the directive leaves the line silent).

- **P74 — Structured compile-log entries jump to source.** After a render whose real subprocess log contains a line-tagged message, the Compile Log pane presents a structured entry (observed via the `structuredLog()` hook) with a parsed `{line, severity, message}` matching that log line; activating the entry moves the editor cursor to exactly that source line (observed via `cursorOffset()`/`goToLine`). The RAW log (P11) is still present and unchanged.
  Admissible because it fails on: a raw-only log (no structured entry exists, only the unparsed text — P11's surface); a parse that drops the line number (the entry has no jump-target, activation is a no-op); and a jump to the wrong line (the cursor lands somewhere other than the parsed line).
  It does NOT subsume or weaken P11 — P11's raw-log assertion runs unchanged alongside this.

- **P75 — RESERVED, HELD.** Real-ChkTeX-on-transient-`.tex` with `.tex`→markdown line mapping.
  HELD pending the `sourcepos`/scroll-sync reader decision; not RED until that gate opens.
  Reserved here so the obligation number is not reused.

- **P76 — RESERVED.** Spare within the Phase-A block (e.g. a buffer-wide diagnostic-count status indicator, or a delimiter-imbalance summary in the status cluster) if A.5 is promoted from consolidation to a first-class observable.
  Not specified until needed.

### Milestone — Phase B: snippet engine (P77–P83)

P77–P83 (added 2026-06-18) cover the Phase B "snippet engine" milestone, authored from [[phase-b-snippet-engine]]. The engine wraps the existing P51/P52/P59 completion/popup/bar base — it must not displace the LaTeX completions or the insertion-bar path, which stay green every commit.
Each obligation is an exact externally-observable happy-path state, driven by the real app via the `tauri-plugin-playwright` harness and the `EditorPane` harness methods (`typeInEditor`, `acceptCompletion`, `appendAtEnd`, `insertSnippetByTrigger`, plus new surfaces for autotrigger/visual-wrap), against real config and the user's real quicktex source.
Each is admissible only if it FAILS on a plausibly broken app — a mode-blind, no-op, literal-token, or flattening engine.
A declared-but-unparseable dictionary is a hard, loud failure (a toast), never a silent-empty engine.

- **P77 — Math-mode-only expansion (keystone).** Config declares a dictionary with the SAME trigger mapped to a prose body and a math body (mode-tagged).
  With the cursor in prose, typing the trigger and accepting expands the PROSE body.
  With the cursor inside `$…$` (or a math environment), typing the SAME trigger and accepting expands the MATH body; the prose body never appears in math and vice versa.
  A math-only trigger is NOT offered at all in prose.
  Admissible because it fails on a mode-blind engine (the same body expands in both zones, or a math-only trigger fires in prose), and on an engine that drops one mode entirely (the trigger is offered in neither zone).

- **P78 — Autotrigger space-expansion, re-arming for chains.** Config declares an autotrigger entry.
  Typing the trigger followed by a space expands the body IN PLACE with no completion popup and no accept keypress (the trigger text is gone, the expansion is at the cursor).
  Immediately typing a SECOND autotrigger + space expands again (the engine re-armed).
  Admissible because it fails on a popup-only engine (the trigger stays literal until an explicit accept), on a one-shot engine (the first autotrigger fires but the second does not, proving no re-arm), and on a no-op (the trigger + space leaves the literal trigger in the buffer).

- **P79 — Regex/postfix trigger with capture group.** Config declares a regex entry whose body references a capture group (e.g. `([a-z])bar` → `\bar{$1}`). Typing `pbar` and triggering expansion yields `\bar{p}` at the cursor (the captured `p` substituted into the body), with the matched trigger text gone.
  Admissible because it fails on a literal-trigger engine (no regex match, `pbar` stays in the buffer), on a capture-blind engine (the body inserts a literal `$1` instead of the captured `p`), and on a no-op.

- **P80 — Mirrored tabstops.** Config declares an entry whose body repeats a tabstop number in two positions (e.g. an environment whose name is mirrored into its closing fence/`\end`). Expanding the entry and typing the environment name into the first slot makes the SAME text appear at the mirrored position live, without a second keystroke there.
  Admissible because it fails on a single-tabstop engine (the second position stays empty or holds the literal `${N}`), and on a no-mirror engine (typing into the first slot does not update the second).

- **P81 — Standard quicktex format consumed directly, mode-split + tabstops preserved.** The editor loads the user's real two-map quicktex source (`g:quicktex_prose` + `g:quicktex_math`) directly — no bespoke flattened intermediate.
  A short trigger present in BOTH maps offers its prose body in prose and its math body in math (the prose/math mode-split survived interop, i.e. the 281→262 flattening loss is gone), and a discriminating multi-tabstop entry (one that carried `<+++>` + `<++>` in the source) expands with its ordered tabstops intact (the secondary `<++>` is a real `${N}` slot, not deleted).
  Admissible because it fails on a flattening loader (the mode-split is gone — the same body in both zones), on a loader that drops one source map, and on a lossy tabstop mapping (the multi-tabstop entry expands with its secondary slot deleted).

- **P82 — Snippet variables resolve at expansion.** Config declares an entry whose body contains `$CLIPBOARD` and `$CURRENT_DATE`. With known text on the system clipboard, expanding the entry inserts a body where `$CLIPBOARD` is replaced by the real clipboard text and `$CURRENT_DATE` by the host date (not the literal tokens).
  Admissible because it fails on a literal-token engine (`$CLIPBOARD` appears verbatim in the buffer), and on a no-op.

- **P83 — Transform node + visual-selection wrap.** (a) An entry with a transform mirror (`${1/.../.../}`) derives the dependent slot from the source slot (typing into the source produces the transformed text in the dependent position).
  (b) With a real selection active, expanding a `${VISUAL}` entry wraps exactly the selected text in the expansion (select `foo`, trigger → `\emph{foo}`). Admissible because it fails on a transform-blind engine (the dependent slot shows the untransformed source text or the literal `${1/.../.../}`), and on a wrap that discards the selection (the expansion appears but the selected text is gone or not wrapped).

### Milestone — Phase C: citations (P84–P89)

P84 (added 2026-06-18) covers the Phase C sub-milestone C1 "bibliography as a single config-declared source", authored from [[phase-c-citations]] (the 2026-06-18 RATIFIED decision).
It is the foundation the rest of Phase C consumes: the bibliography the app cites against is one user-controlled config value, read by both the frontend and the preview, validated to exist at load.
It is an exact externally-observable happy-path state, driven by the real app via the `tauri-plugin-playwright` harness against real config and a real `.bib`/CSL file.
It is admissible only if it FAILS on a plausibly broken app.

P85–P86 (added 2026-06-18) cover the Phase C sub-milestone C2 "citation completion: metadata fuzzy-match + tooltip preview + `@`-trigger", authored from [[phase-c-citations]]. They build on P84's single config-declared bibliography: the candidates and tooltip content are sourced from that same configured bibliography.
Each is an exact externally-observable happy-path state, driven by the real app via the `tauri-plugin-playwright` harness against real config and a real `.bib`/CSL file carrying an entry whose title/author word is absent from its key.
Each is admissible only if it FAILS on a plausibly broken app.
P88–P89 are reserved for the remaining Phase-C sub-milestones (C4–C5) and are not specified until ratified.

P87 (added 2026-06-18) covers the Phase C sub-milestone C3 "cross-file label / `\cref` completion via project-root harvesting", authored from [[phase-c-citations]]. It is an independent completion source — labels are harvested from the project's markdown files, not from the bibliography, so it does not consume P84's configured bib.
It is an exact externally-observable happy-path state, driven by the real app via the `tauri-plugin-playwright` harness against a real project containing two markdown files where a label/anchor is defined in one file and referenced from the other.
It is admissible only if it FAILS on a plausibly broken app.

P88 (added 2026-06-18) covers the Phase C sub-milestone C4 "per-file `bibliography:` override", authored from [[phase-c-citations]]. It refines P84's single config-declared bibliography into a per-file-overridable source: a document may declare its own bibliography in its YAML frontmatter, and that file-local declaration takes precedence for that document while the global config bibliography remains the source for documents that do not.
It is an exact externally-observable happy-path state, driven by the real app via the `tauri-plugin-playwright` harness against real config and a real sibling `.bib` file carrying a key absent from the global config bibliography.
It is admissible only if it FAILS on a plausibly broken app.

P89 (added 2026-06-18) covers the Phase C sub-milestone C5 "references / bibliography preview sidebar tab", authored from [[phase-c-citations]]. It is the document's live "what this doc cites so far" panel — a sidebar tab that lists only the keys the current document actually cites, each rendered as a formatted reference in the configured citation style, resolved from the active bibliography (P84's global config bibliography or P88's per-file override).
It rides the same resolved bibliography the preview already renders (the `#refs` block — p27), so the sidebar cannot drift from the preview.
It is an exact externally-observable happy-path state, driven by the real app via the `tauri-plugin-playwright` harness against real config and a real `.bib`/CSL file.
It is admissible only if it FAILS on a plausibly broken app.

- **P84 — Bibliography is a single config-declared source for both the frontend and the preview.** The bibliography the app cites against is one user-controlled config value, `editor.bibliography` — a required, existing-file-validated config key.
  That one value governs BOTH surfaces: the frontend can read the configured bibliography path, AND the preview's resolved references (the `#refs` bibliography that pandoc citeproc produces for the document's cited keys) are resolved from that same configured bibliography.
  Pointing `editor.bibliography` at a DIFFERENT bibliography file changes what the preview resolves — the references the preview renders for a cited key reflect the now-configured file, not a previously baked-in bibliography.
  A missing/non-existent `editor.bibliography` path fails LOUDLY at config load — the app does not reach the editor with an empty or silently-substituted bibliography.
  The CSL (`editor.csl`, the style the resolved references are rendered in) is a required, existing-file-validated config key governed the SAME way: readable by the frontend, driving the preview's reference rendering, and fail-loud-at-load on a missing path.
  Admissible because it fails on an app where the bibliography is a literal the frontend cannot read (the frontend has no path to the configured bibliography, so the one config value does not govern the frontend surface), on a silent-accept of a missing bibliography (a non-existent configured path is tolerated and the app boots with no bibliography rather than failing loud at load), and on a split-source app where the preview resolves references from a bibliography OTHER than the configured value (pointing `editor.bibliography` at a different file leaves the preview's resolved `#refs` unchanged, proving a second bibliography drives the preview).
  It passes only when the one config value drives both the frontend-readable surface and the preview's resolved references, and the same holds for `editor.csl`.

- **P85 — `@`-trigger citation completion with metadata fuzzy-match.** Typing `@` in a citation trigger position — at line start, immediately after whitespace, or immediately after an opening bracket — opens the editor's completion tooltip offering candidates drawn from the configured bibliography.
  The candidate list filters against the entry's author, year, and title, not the cite key alone: a query that matches a word appearing in an entry's TITLE surfaces that entry even when the entry's KEY does not contain that word (proving the match string is built from the bibliographic metadata, not the key).
  Accepting a candidate inserts pandoc citation syntax at the cursor — a bracketed `[@<key>]` or a narrative `@<key>` reference to the chosen entry — not the bare cite key as plain text and not a literal label.
  Admissible because it fails on a key-only matcher (the title-word query that does not appear in any key yields no candidate, so metadata is not being matched), on a no-trigger wiring (typing `@` in a trigger position does not open the completion tooltip), and on a literal insert that is not pandoc citation syntax (accepting a candidate leaves the bare key or a non-citation literal at the cursor rather than a `[@<key>]` / narrative `@<key>` reference).

- **P86 — Citation completion option previews the bib entry before insert.** Each citation completion option carries an info tooltip whose rendered content shows the entry's author, year, and title — the fields the user verifies before inserting — sourced from the configured bibliography file.
  Hovering/selecting an offered candidate surfaces that info tooltip, and the previewed content is the entry's bibliographic metadata, so the user can disambiguate among candidates without inserting first.
  Admissible because it fails on an option with no info tooltip (nothing previews before accept, so the user cannot verify the entry), and on a tooltip that shows only the bare key (the author/year/title metadata the user needs to disambiguate is absent — the preview carries no more information than the option label already shows).

- **P87 — Cross-file label completion spans the whole project.** In a project containing TWO markdown files, a label/anchor — a pandoc `{#id}` heading attribute, a `:::{#id}` fenced-div id, or a `\label{}` — is defined in file A. Opening file B and triggering label completion offers the label defined in the OTHER file (file A), proving the label index spans the whole project, not just the current buffer.
  Accepting the offered label inserts a reference to that label at the cursor.
  Admissible because it fails on a current-buffer-only index (file A's label is never offered while editing file B, so the index does not span the project), on a no-op source (no labels are offered at all, so nothing from any file surfaces), and on an insert that does not reference the chosen label (accepting leaves text at the cursor that does not refer to the selected label).

- **P88 — Per-file `bibliography:` frontmatter override, with the global config bibliography as the source for files without it.** A document whose YAML frontmatter declares a `bibliography:` pointing at a sibling `.bib` file that contains a cite key ABSENT from the global config bibliography governs that document's citations: with that document open, citation completion offers the file-local key — the key that exists only in the frontmatter-declared bibliography — proving the per-file override is in effect for the open document.
  A document WITHOUT the frontmatter `bibliography:` key still offers the global config bibliography's keys, proving the global config bibliography remains the source for documents that do not override it, not a hole left behind by the override mechanism.
  A frontmatter `bibliography:` pointing at a non-existent path fails LOUDLY with a visible error — the override is never silently treated as an empty source.
  The `bibliography:` key is pandoc's own native per-file metadata key, consumed as authored.
  Admissible because it fails on an app that ignores the frontmatter `bibliography:` key (the file-local key — present only in the frontmatter-declared bibliography — is never offered while that document is open, so the override has no effect), on an override mechanism that drops the global config bibliography for documents without the frontmatter key (a document with no `bibliography:` frontmatter offers none of the global config bibliography's keys, so the global source has become a hole), and on a non-existent override path treated silently (a frontmatter `bibliography:` pointing at a missing file produces no visible error and an empty citation source rather than failing loud).

- **P89 — References sidebar reflects only the document's cited keys, rendered in the configured citation style.** With the references sidebar tab active, the panel lists only the references the current document actually cites — not the whole bibliography.
  Citing a key in the buffer makes that key's reference APPEAR in the panel, rendered with its bibliographic text (author/year/title), not as the bare cite key.
  A key that exists in the active bibliography but is NOT cited anywhere in the buffer does NOT appear in the panel — the panel lists what the document cites, not the entire bibliography.
  The rendering uses the configured citation style: the formatted reference the panel shows for a cited key matches the preview's resolved bibliography (the `#refs` entry pandoc citeproc produces for that same key — p27), so the panel cannot drift from the preview.
  Admissible because it fails on a static/empty panel (citing a key in the buffer adds nothing to the panel, so the panel does not reflect the document's citations), on a panel that lists the WHOLE bibliography rather than only the cited keys (a key present in the active bibliography but not cited in the buffer appears in the panel, proving the panel is not scoped to what the document cites), and on a panel that shows bare cite keys with no resolved bibliographic text (a cited key appears as its raw key rather than its styled author/year/title reference, proving no citation-style rendering is applied).

### Milestone — Phase D: figures & TikZ (P90–P100)

**2026-06-20 REALIGNMENT (issue #2 — ratified by the user).** Phase D built tikz as an INLINE-IN-MARKDOWN subsystem: a tikzcd.lua filter compiled each fenced tikz block to an SVG inside the markdown preview, fed by a config-injected `[figures]` palette/template (`tikzstyles`/`tikzdefs`/`template`) the renderer forwarded as `{tikzstyles}`/`{tikzdefs}`/`{figure_template}` env/placeholders. The user ruled this a bespoke injection drift: tikz is the SAME render primitive as markdown — "pandoc + a template" — applied to a tikz FILE, with the template OWNING its preamble (`\usepackage{dzg-tikz}` over the user's styles tree). The inline-in-markdown machinery and the entire injection surface are REMOVED; a tikz file now renders standalone via the `tikz-renderer` sibling plugin (`render_tikz`, the user-owned `standalone-tikz.tex` template → pdflatex → pdf2svg → inline SVG), selected by file type exactly as markdown selects the html5 renderer. This fixes the issue-#2 boot failure at the root: there is no `[figures]` `ExistingFile` and no `shared.*` asset left to drift or go missing.

Disposition of the Phase D obligations under this realignment:

- **P90 (D-1 parser) — KEPT.** The owned-tikz parser (`cargo test tikz_roundtrip`) is model-only; it serves authoring/copy in file mode. Unchanged.
- **P100 (D-0 inline tikz→SVG in markdown) — REPLACED** by the new file-mode obligation **P128** below (a tikz FILE renders standalone as an inline SVG via the user-owned template). Burden transferred: "tikz source compiles to a real vector figure in the live preview, raw source not echoed" is re-proven against the file render path.
- **P91 (D-2 shared `.tikzstyles`/`.tikzdefs` palette) — RETIRED.** The injected shared palette is the invented mechanism. The legitimate underlying need ("the user's styles govern figures") is carried by P128: the template owns its preamble via `\usepackage{dzg-tikz}`, so a style the user defines in the styles tree governs the rendered figure. No config palette, no `shared.*` asset.
- **P92 (D-3 config-declared per-figure template) — RETIRED.** The config `template` key is the invented mechanism. The legitimate need ("the template governs the compile") is carried by P128: the user-owned `standalone-tikz.tex` (editable, at a fixed path) is what every tikz file compiles under.
- **P93 (D-4 source↔preview jump) — RETIRED as ill-conceived.** A source-line → rendered-SVG-element mapping is not well-defined (pdf2svg has no per-node identity; procedural/loop tikz has no 1:1 correspondence). The scaffolding is deleted. See the realignment section's P93/P109 entry.
- **P94 (D-5 tikz-command DB) — KEPT.** Authoring assist, independent of the render mechanism.
- **P95 (D-6 inline figure-compile error log) — REPLACE PENDING.** File-mode compile errors surface via the `tikz-renderer` plugin's `log`/stderr; a new spec asserting a malformed tikz file surfaces a clickable error replaces the inline `tikzfigurelog.ts` path. Outstanding.
- **P96 (D-7 dual-asset registry for non-tikz figures) — KEPT.** Orthogonal to tikz.
- **P97 (D-8 subgraph copy) — KEPT.** Model-only clipboard operation.
- **P98 (D-9 watch-file reload) — REWORK PENDING.** Generic watch/reload; re-point its fixture from an inline-figure markdown doc to a tikz FILE rendered via `render_tikz`. Outstanding.
- **Phase G arXiv (the tikz-externalize legs P116/P117 + capstone) — REWORK PENDING.** `arxiv-export/export.sh` drove `tikzcd.lua` to externalize inline tikz; with inline tikz gone, the bundle references precompiled tikz files. Outstanding.

UPDATE (2026-06-20) — full spec disposition so NO spec tests a removed mechanism:

- **RETIRED (feature genuinely deleted; specs + provisioning removed):**
  `p100-tikz-render` (inline render → replaced by P128), `p101-shared-tikzstyles` (P91 injection), `p102-perfigure-template` (P92 injection), `p105-tikz-error-map` (P95 inline figure-log → superseded by the standard Compile Log), `p109-tikz-line-jump` (P93 jump → RETIRED as ill-conceived: no source-line→SVG-element mapping exists), and the arXiv inline-tikz-externalize specs `p124-arxiv-tikz` (P116), `p125-arxiv-figure-gate` (P117), `p127-arxiv-capstone` (its tikz-externalize leg) — there is no inline tikz to externalize. `p126-arxiv-cleaner` is kept (non-tikz).
- **REPLACED:** P100 → **P128** authored as `tests/proof/p132-tikz-file-render.spec.ts` (+ provisioning: a real `figure.tikz` opened via the discovery matrix → inline SVG, raw source absent; verified provisioning + a real tikz→SVG render).
- **REWORKED to the new model (no longer red):** `p108-watch-file-reload` (P98) → a generic markdown text-marker witness (the watch-reload is renderer-independent); `p121-slides-preview` → drives the shipped revealjs-renderer via the render-target selector (`setRenderTarget('revealjs-renderer', null)`) — verified the revealjs render.sh emits a real `.reveal > .slides > section` deck. The frontend inline figure-compile log (`tikzfigurelog.ts` + the TikZ Log tab) is removed.
- **P93/P109 (source↔preview jump) — RETIRED as ill-conceived (not deferred).** The obligation assumes a source-line → rendered-SVG-element correspondence, but `pdf2svg` output carries NO per-node identity and procedural tikz (e.g. a loop drawing an N×N grid) has no 1:1 line↔element mapping at all — the feature is not well-defined. It was agent-invented. The entire scaffolding is DELETED: `tikzjump.ts`, the App.svelte jump handler + harness hooks, the EditorPane Ctrl+J/Ctrl+T keybindings + `ownedTikzEnvelopeText`/`refreshTikzModel`/`placeCursorOnTikzNodeLine`/`cursorTikzNodeName`/`__PPE_TIKZ_MODEL__`. (P90 parser + P97 subgraph copy, which share `parse_tikz`, are unaffected.)
- **FUTURE obligations (deferred, no red spec):** the P130 beamer (slides→pdf) export-template leg, and P131/P132 (`.tex`/`.bib` cells).

All reworked/authored specs are verified as far as possible without a display (provisioning boots doctor-clean, render.sh produces the asserted output); the full `just proof` E2E run still requires the GUI session.

P100 (added 2026-06-18) covers the Phase D foundational sub-milestone D-0 "activate + prove the tikz→SVG preview compile pipeline", authored from [[phase-d-figures-tikz]] (the 2026-06-18 SCOPE DISCOVERY note).
It is the DISCOVERED FOUNDATION beneath D-2/D-3/D-6: those obligations assume a figure-compile-to-SVG preview seam, but investigation found that seam DORMANT — the tikz preview compile path is a required-on-disk filter that is deliberately never loaded into the renderer command, no proof has ever exercised tikz→SVG rendering, and the tikz toolchain is not surfaced by the doctor battery.
D-2/D-3/D-6 cannot be RED until P100 is green.
Like the playwright-driven obligations it states the OBSERVABLE behaviour ONLY — no module, filter, env-var, or generator names (the activation wiring belongs to the implementer, not this obligation).
P100 is proven through the real app on a real display (a tikzpicture in the open document producing an inline SVG in the live preview) AND through the startup doctor battery (the tikz rendering toolchain surfaced as a hard dependency that fails loud when absent).
It is admissible only if it FAILS on a plausibly broken app.

P90 (added 2026-06-18) covers the Phase D sub-milestone D-1 "tikz-subset parser (round-trip parse↔serialize)", authored from [[phase-d-figures-tikz]]. It is the KEYSTONE obligation of the phase: the foundation D-4 (source↔preview line jump, P93) and D-8 (copy subgraph as tikz, P97) ride on the owned structured graph model it asserts.
It states the OBSERVABLE round-trip behaviour ONLY — no module names, no crate choice (the spec design, parser library, and serializer story belong to the implementer, not this obligation).
Unlike the playwright-driven Phase A–C obligations, P90 is the one pure-Rust unit-testable seam in this phase: it is proven by `cargo test` over real `.tikz` fixtures (no webview, no display), asserting ROUND-TRIP EQUALITY on real TikzIt-class source — never that a parse function merely exists.
It is admissible only if it FAILS on a plausibly broken app.

P91 (added 2026-06-18) covers the Phase D sub-milestone D-2 "shared `.tikzstyles` + `.tikzdefs` palette for the figures dir", authored from [[phase-d-figures-tikz]]. It rides P100's now-active tikz→SVG compile seam: the shared style/defs files are `\input` by the figure compile that produces the preview SVG. It states the OBSERVABLE behaviour ONLY — no module, config-key, filter, or env-var names (the config wiring and the `\input` mechanism belong to the implementer, not this obligation).
It is proven through the real app on a real display (a figure using a style defined only in the shared file renders accordingly, and changing/removing that style changes the rendered figure) — never by an existence check that a style file is present on disk.
The shared `.tikzstyles` and `.tikzdefs` are config-declared, existing-file-validated paths; a missing one fails loud at config load.
It is admissible only if it FAILS on a plausibly broken app.

P92 (added 2026-06-18) covers the Phase D sub-milestone D-3 "swappable per-figure preamble template (`<>` placeholder)", authored from [[phase-d-figures-tikz]]. It generalizes the FIXED vendored `standalone-tikz.tex` preamble (which already `\input`s the shared `.tikzstyles`/`.tikzdefs` of P91 and `\usepackage{tikzit}`) into a config-declared, existing-file-validated template whose single `<>` marker (the QTikz `.pgs` `TemplateReplaceText` convention) is where the figure source is substituted before compile — letting a figure declare its OWN `\usetikzlibrary`/macros independent of the fixed pandoc-filter preamble.
It rides P100's now-active tikz→SVG compile seam and the P91 shared-style model.
It states the OBSERVABLE behaviour ONLY — no module, config-key, filter, or env-var names (the template-substitution wiring belongs to the implementer, not this obligation).
It is proven through the real app on a real display (a figure requiring a library/macro present only in the configured template compiles and renders, and swapping the config to a template lacking that library makes the SAME figure fail to compile / render no figure) — never by an existence check that a template file is present on disk.
The per-figure template is a config-declared, existing-file-validated path; a missing one fails loud at config load.
It is admissible only if it FAILS on a plausibly broken app.

P93 (added 2026-06-18) covers the Phase D sub-milestone D-4 "source↔preview line jumping for owned tikz (TikzIt Ctrl+J jump-to-source / Ctrl+T re-parse)", authored from [[phase-d-figures-tikz]]. It is the round-trip UX the Tier-3 "right-click to edit owned tikz" needs but never specified: it rides the D-1 owned tikz model (P90 — nodes carrying `name` + `(x,y)` coordinate) as the graph the jump maps FROM, and P100's now-active tikz→SVG preview compile seam as the rendered figure it maps INTO. It states the OBSERVABLE behaviour ONLY — no module, keybinding-registration, SVG-annotation, coordinate-mapping, channel, or parser internals (the mechanism by which a specific node SOURCE line is mapped to the specific MATCHING rendered element, and the mechanism by which a re-sync re-parses the edited model, belong to the implementer, not this obligation).
It is proven through the real app on a real display: with owned tikz source open whose figure is rendered in the live preview, invoking the jump from the cursor positioned on a specific NODE source line selects/scrolls the preview to the rendered element corresponding to THAT node (not another node), and editing the tikz source then invoking the re-sync updates the preview to reflect the edited model — never by an assertion that a keybinding merely exists.
The jump MUST resolve a specific node-line→specific-rendered-element correspondence; an approximate scroll, a no-op, or a wrong-element jump is a failure, never a soft "best effort."
It is admissible only if it FAILS on a plausibly broken app.

P94 (added 2026-06-18) covers the Phase D sub-milestone D-5 "declarative tikz-command snippet database", authored from [[phase-d-figures-tikz]]. It upgrades the P56 insertion-bar tikz surface from bare tikz/tikzcd scaffolds to a categorized, cursor-aware command corpus sourced from a config-declared vendored tikz-command DB (the QTikz `tikzcommands.json` `{name, description, insert, type}` model), seeding BOTH the insertion-bar tikz palette AND a CM6 editor completion source — riding the P51 composable completion registry and the P59 bar-dropdown surface (the same dual surface P52/P59 already split: autocomplete-popup vs bar-dropdown).
It states the OBSERVABLE behaviour ONLY — no module, config-key, completion-source, or filename internals (the DB-load and surface-seeding wiring belongs to the implementer, not this obligation).
It is proven through the real app on a real display (a command present ONLY in the config-declared DB is surfaced and, when chosen, inserts its declared insert body with the cursor at the declared offset; pointing config at a DIFFERENT DB surfaces THAT DB's commands) — never by an existence check that a DB file is present on disk.
The tikz-command DB is a config-declared, load-validated path: a missing one fails loud at config load, and an unreadable/malformed DB is a hard visible error, never a silent empty palette.
It is admissible only if it FAILS on a plausibly broken app.

P95 (added 2026-06-18) covers the Phase D sub-milestone D-6 "LaTeX-error→source-line mapping + compile-log tab inside TikZ mode", authored from [[phase-d-figures-tikz]]. It closes the D-3 design gap where a figure-compile FAILURE currently DROPS that single figure and logs only to stderr (the error is NOT user-visible): the figure-compile diagnostic must reach the app and surface as a clickable TikZ-mode log entry.
It LEVERAGES the standard `file:line:message` LaTeX error format the figure compile already emits (pdflatex `-interaction=nonstopmode`) — the figure-compile analog of the pandoc compile log P11 surfaces, distinct from P11 — parsing no bespoke error shape, and reuses the editor's existing goto-line / setCursor jump surface (the P74/P75-class jump).
It states the OBSERVABLE behaviour ONLY — no module, channel, log-field, parser, or generator names (the wiring by which the figure-compile diagnostic reaches the app, the parse of the `file:line:message` records, and the cursor-jump mechanism belong to the implementer, not this obligation).
It rides P100's now-active tikz→SVG compile seam: the error is the FAILURE outcome of that same figure compile.
It is proven through the real app on a real display (a malformed tikz construct introduced into a figure produces a compile-log entry in a TikZ-mode log tab/panel, and ACTIVATING that entry lands the editor cursor on the exact offending tikz source line) — never by an existence check that a log panel is present.
It is admissible only if it FAILS on a plausibly broken app.

P97 (added 2026-06-18) covers the Phase D sub-milestone D-8 "copy selected subgraph as tikz to clipboard" (the TikzIt copy-subgraph model), authored from [[phase-d-figures-tikz]]. It reuses the D-1 owned tikz model (P90) — the SAME parser and the SAME canonical `Graph::to_tikz()` serializer that P90 round-trips — to emit a SELECTED subgraph of owned tikz source as deterministic canonical tikz onto the SYSTEM clipboard, a sibling of the existing clipboard-image command (P62). It states the OBSERVABLE behaviour ONLY — no module, command, or clipboard-plugin internals (the wiring belongs to the implementer, not this obligation).
It is proven through the real app on a real display (a selection over a region of the owned tikz source is copied, then the system clipboard is read by an INDEPENDENT process and re-parsed by the app's own tikz parser back into the SELECTED subgraph).
A selection that is not parseable tikz is a LOUD error, never a silent raw-text copy.
It is admissible only if it FAILS on a plausibly broken app — a whole-graph copy, a raw-selected-text (non-tikz) copy, or a serialization the parser cannot re-parse.

P96 (added 2026-06-18) covers the Phase D sub-milestone D-7 "dual-asset registry for NON-tikz figures (Ipe `.ipe`/PDF, Inkscape `.svg`)", authored from [[phase-d-figures-tikz]]. It is PURE external-tool interop: Ipe owns NO tikz (negative finding, [[parity-research/ipe]]), so there is NO owned parser here and tikz extraction is NEVER attempted on a non-tikz figure.
A non-tikz figure is a DUAL asset — an editable SOURCE (`.ipe`/`.svg`) paired with the included RENDER (PDF/SVG) — and the "edit this figure" action launches the real diagram-tool editor on the SOURCE via the plugin firewall (a `configure_plugin`-shaped detached launch; the launch argv lives in a diagram-tool PLUGIN, not app core — the app stays tool-agnostic).
The source↔render pairing is persisted in a host-filesystem registry sidecar (an XDG-state JSON, the `session.json`/`fold-state.json` read-/save-state pattern, fail-loud parse — NOT browser storage); a missing source is a loud error.
It states the OBSERVABLE behaviour ONLY — no module, plugin-manifest, registry-schema, or argv-substitution internals (the wiring belongs to the implementer, not this obligation).
A real Ipe/Inkscape GUI cannot be asserted headless; the WIRING is proven through the real plugin firewall by a real diagram-tool plugin whose launch command is a thin script that RECORDS the argv it received (writing the path it was handed to a sentinel file) — a REAL plugin exercising the REAL launch path, not a mock — and the recorded path is the observable proving the SOURCE, not the render, was passed.
It is admissible only if it FAILS on a plausibly broken app.

P99 (added 2026-06-18) covers the Phase D sub-milestone D-10 "SVG/PDF-vector inclusion path for external-editor figures", authored from [[phase-d-figures-tikz]]. It is the non-tikz sibling of the clipboard-image path P62: where P62 persists a clipboard PNG into the configured global figures dir and inserts a markdown image reference to it, P99 does the same for an external-editor-produced VECTOR asset — an Ipe/Inkscape-emitted SVG/PDF (Ipe owns no tikz; negative finding, [[parity-research/ipe]], so this is PURE interop on the vector asset, never tikz extraction).
It reuses P62's figures-dir resolution and atomic-write discipline in `clipboard.rs` (the SAME `config.directories.figures` ExistingDir, the SAME stage-and-rename atomic write, the SAME bare-basename guard) and additionally REGISTERS the inserted vector asset in the D-7 / P96 dual-asset registry, recording the included render alongside its editable source so the inserted figure is later re-openable in its source tool.
It states the OBSERVABLE behaviour ONLY — no module, command, registry-schema, or argv internals (the wiring belongs to the implementer, not this obligation).
It is proven through the real app on a real display, with the on-disk write and the inserted reference asserted by an INDEPENDENT process reading the configured global figures dir and the editor buffer (the P62 discipline) — never via the app's own report.
It is admissible only if it FAILS on a plausibly broken app.

P98 (added 2026-06-18) covers the Phase D sub-milestone D-9 "watch-file reload of an owned figure", authored from [[phase-d-figures-tikz]]. It closes the launch→edit→return loop that D-7 (P96) opens: D-7 launches the real diagram-tool editor on an owned figure SOURCE, and P98 is the return half — when that external tool REWRITES the open owned figure on disk with NEW content, the in-app preview RELOADS to reflect it.
It REUSES the EXACT P48 fingerprint primitive (the FNV-1a content hash + nanosecond mtime that `fsops.rs` returns on read/write and that P48 already uses to detect external changes to the open file): a reload fires precisely when the open owned figure's on-disk fingerprint DIVERGES from the stored one, and an unchanged file (same fingerprint) triggers NO reload — there is NO new change-detection scheme.
It states the OBSERVABLE behaviour ONLY — no module, watcher, channel, or fingerprint-field internals (the wiring belongs to the implementer, not this obligation).
It is proven through the real app on a real display, where the external rewrite is performed by an INDEPENDENT process writing the file (the P48/P49 idiom for durable host-FS state — NOT the app writing it), and the reload is observed as the preview reflecting the NEW on-disk content (not the stale render).
It is admissible only if it FAILS on a plausibly broken app — a stale render persisting after the external write (no reload), or a spurious reload firing absent any real change.

### Milestone — Phase E: project navigation (P101–P106; E1 workspace search, E2 structural motions, E3 command palette + Ctrl+P quick-open, E4 surround/toggle edits, E5 frontmatter editor)

P101–P102 (added 2026-06-19) cover the Phase E headline sub-milestone E1 "global full-text WORKSPACE search", authored from [[phase-e-project-navigation]] (the 2026-06-19 RATIFIED renumber: the plan's drafted P100/P101 COLLIDE with Phase D's P100 / D-0, so E1 maps to P101 + P102; Phase E specs continue p110+). They are the new `search` sidebar view (a sibling of the Phase-C references panel in `SIDEBAR_VIEWS`, riding the P18 activity-bar model built to hold more views) — a global content search across the open project, distinct from in-file find and from filename filtering.
P101 is the content-search keystone (boolean grammar + click-to-open-at-line); P102 refines it with per-directory restriction and a relevancy heatmap.
The witness uses the shared fixture extended to a multi-file project.
They state the OBSERVABLE behaviour ONLY — no search-backend, plugin, ripgrep, query-translation, scoring, or heat-class-rendering internals (the wiring belongs to the implementer, not these obligations).
They are proven through the real app on a real display, with the opened file's cursor line and the result-list contents read independently of the app's own report.
Each is admissible only if it FAILS on a plausibly broken app.

P103 (added 2026-06-19) covers the Phase E sub-milestone E2 "section / environment / math-zone keyboard MOTIONS", authored from [[phase-e-project-navigation]] (the 2026-06-19 RATIFIED renumber maps E2 to P103). It is the in-buffer fast structural-navigation path a sidebar jump does not give — vimtex's section/environment/math-zone motion CAPABILITIES ported as CM6 editor commands (NOT vim keystrokes): next/prev section over the `markdownOutline` headings, next/prev environment over the fenced-div blocks, next/prev math zone over the `$`/`$$`/`\(`/`\[` math regions, each relative to the cursor's current position.
It states the OBSERVABLE behaviour ONLY — no keymap-block, command-name, outline, syntax-tree, or goToLine internals (the wiring belongs to the implementer, not this obligation).
It is proven through the real app on a real display, with the editor cursor position read independently of the app's own report.
It is admissible only if it FAILS on a plausibly broken app.

P104 (added 2026-06-19) covers the Phase E sub-milestone E3 "command palette (Ctrl+Shift+P) + Ctrl+P quick-open rebinding", authored from [[phase-e-project-navigation]] (the 2026-06-19 RATIFIED renumber maps E3 to P104). It FIXES a current-state bug AND re-homes the command surface behind the plugin firewall: `App.svelte` currently binds Ctrl+P/Cmd+P to an app-owned `CommandPaletteModal.svelte`, which is BOTH the wrong binding (Ctrl+P is VSCode's quick-open / file finder, not the command palette) AND the wrong delivery surface (the palette must live behind the firewall per the OS-integration-as-plugin doctrine, not as app chrome).
E3 rebinds Ctrl+P → quick-open (a fzf firewall file finder over the workspace file list → open the picked file), moves the command palette to Ctrl+Shift+P (a fzf firewall picker fed the `paletteCommands()` catalog including E2's named motion commands → run the picked command), and DELETES `CommandPaletteModal.svelte` entirely (replaced, not deprecated, no fallback).
The transitional `p40-command-palette.spec.ts`, which currently asserts the OLD Ctrl+P→app-palette behaviour, is MIGRATED to the new behaviour — replaced with an aligned correction, never a hollow deletion.
Because fzf is an interactive TUI that cannot be driven headless (the same constraint as the D-7 diagram-tool GUI launch, P96), the WIRING is proven through the real plugin firewall by a REAL firewall plugin whose picker step is SCRIPTED to return a deterministic selection (it reads a fixture-configured selection / env and emits the chosen command id / file path on stdout) — a REAL plugin exercising the REAL firewall, NOT a mock in app logic; the real fzf picker is the production UI, and the proof substitutes a non-interactive selection-returning plugin via config.
The app then RUNS the returned command / OPENS the returned file; the observable is the command actually executing (the buffer folds) / the file actually opening, proving the palette RUNS the selection, not merely lists it.
It states the OBSERVABLE behaviour ONLY — no keybinding-block, plugin-manifest, picker-protocol, or run-dispatch internals (the wiring belongs to the implementer, not this obligation).
It is proven through the real app on a real display, with the folded ranges and the opened file's editor state read independently of the app's own report.
It is admissible only if it FAILS on a plausibly broken app.

P105 (added 2026-06-19) covers the Phase E sub-milestone E4 "environment / command surround + toggle" (the plan's drafted P104, RENUMBERED to P105 by the 2026-06-19 RATIFIED renumber so it does not collide with E3's P104), authored from [[phase-e-project-navigation]]. It PORTS vimtex's surround/toggle CAPABILITIES (`tse` rename-environment, `tsf` toggle-fraction, `dsd` delete-delimiter-pair) as named CM6 editor commands (NOT vim keystrokes), reusing E2's structure-walking (the `markdownOutline` fenced-div structure + the `latexLanguage` syntax tree the E2 motions already walk) and the existing `EditorPane` edit primitives (`wrapSelection`/`prefixLines`/the `command()` dispatcher).
It is the IN-PLACE EDIT counterpart to E2's read-only motions: each command transforms the EXISTING enclosing structure at the cursor, never inserting a parallel one.
It states the OBSERVABLE behaviour ONLY — no command-name, keymap-block, node-walk, or edit-primitive internals (the wiring belongs to the implementer, not this obligation).
It is proven through the real app on a real display, with the resulting buffer contents read independently of the app's own report.
It is admissible only if it FAILS on a plausibly broken app.

P106 (added 2026-06-19) covers the Phase E sub-milestone E5 "structured YAML frontmatter editor" (the plan's drafted P105, RENUMBERED to P106 by the 2026-06-19 RATIFIED renumber so it does not collide with E4's P105), authored from [[phase-e-project-navigation]]. It is a MODAL surface (the sibling shape of `PromptModal`/`SettingsModal`) for the document's Pandoc YAML frontmatter — the leading `---` … `---` block at the buffer head — mapping the known fields title / author / date / bibliography / csl to form inputs.
It LEVERAGES the maintained `yaml` npm package (already a dependency from Phase C / C4's per-file `bibliography:` override, P88) to PARSE the existing `---` block on open and RE-EMIT it on confirm — no bespoke YAML parser/serializer, the document's native Pandoc frontmatter format consumed as authored.
The keystone guarantee is BLOCK-LOCALITY: on confirm only the frontmatter block is rewritten — the document BODY is byte-for-byte unchanged — and a document with NO frontmatter gains a well-formed `---` block on confirm.
The `bibliography:` field intersects Phase C's per-file bib override (P88) — the field is editable here, but the Phase-C wiring is not (re-)implemented by this obligation.
It states the OBSERVABLE behaviour ONLY — no modal-component, yaml-parse, field-mapping, or block-splice internals (the wiring belongs to the implementer, not this obligation).
It is proven through the real app on a real display, with the on-disk/buffer frontmatter block and the document body read independently of the app's own report.
It is admissible only if it FAILS on a plausibly broken app.

- **P90 — tikz-subset parser round-trips real source.** Parsing a real TikzIt-class `.tikz` fixture — a `tikzpicture` envelope carrying layered nodes and edges (a `\begin{pgfonlayer}{nodelayer}` block of `\node[<props>](<name>) at (<x>,<y>){<label>};` and a `\begin{pgfonlayer}{edgelayer}` block of `\draw[<props>](<src>) to (<tgt>);`), per-node/per-edge styles and properties, node labels, and a bounding-box `\path … rectangle …` — into a structured graph model and serializing that model back yields canonical tikz source that RE-PARSES to a STRUCTURALLY EQUAL model (same nodes with the same names, coordinates, props, and labels; same edges with the same endpoints and props; same layers and bounding box), and the serialization is STABLE across the round-trip (serializing the re-parsed model reproduces the same canonical source).
  A MALFORMED picture is rejected with a LOUD structured error that names the offending line/token — never silently accepted as an empty or partial graph, and never silently soft-reverted in the parser library layer.
  Admissible because it fails on a parser that DROPS content (a node, edge, property, or label present in the fixture is absent from the re-parsed model, so the structures are not equal), on a serializer that emits NON-RE-PARSEABLE source (the canonical output cannot be parsed back, or parses to a different structure, so the round-trip breaks), on an UNSTABLE serialization (re-serializing the re-parsed model produces different source than the first serialization, so the canonical form is not fixed), and on a parser that ACCEPTS MALFORMED input (a malformed picture yields an empty/partial graph with no error, instead of a loud structured error naming the offending line/token).
  It is NOT satisfied by an assertion that a parse function merely exists — a test that would still pass on a parser that drops nodes/edges/props/labels, or on a serializer whose output cannot be re-parsed, is inadmissible.

- **P91 — a tikz style defined only in the shared `.tikzstyles` palette visibly determines a rendered figure's appearance in the preview.** With a figure open in the live preview that uses a named tikz style, and that style defined ONLY in the config-declared shared `.tikzstyles` file (which the figure compile `\input`s alongside the shared `.tikzdefs`), the figure renders in the preview ACCORDING TO that shared style — the style the shared file declares for that name visibly determines the rendered figure's appearance.
  Changing the style's definition in the shared file changes the rendered figure, and removing the style from the shared file changes the rendered figure (the figure no longer renders with that style's appearance).
  The shared `.tikzstyles` and `.tikzdefs` are config-declared, existing-file-validated paths: a missing shared style/defs file fails LOUDLY at config load — the app does not reach the editor with a silently-defaulted or empty shared palette.
  Admissible because it fails if the figure renders IDENTICALLY whether or not the shared style is present (the shared file is not actually consumed by the figure compile, so the declared style has no effect on the render), and if a missing shared style/defs file is SILENTLY DEFAULTED (a built-in or empty palette is substituted) rather than producing a hard config-load failure.
  It is NOT satisfied by an assertion that a style file merely EXISTS on disk — a shared file that is present but never `\input` by the compile would pass an existence check while the figure renders the same with or without it, which is exactly the dormant-palette app this obligation must reject; the proof must observe that the shared style's definition visibly determines the rendered figure and that changing/removing it changes the render.

- **P92 — a tikz library/macro present ONLY in the config-declared per-figure preamble template makes the figure compile, and swapping to a template lacking it makes the SAME figure fail to compile.** With a figure open in the live preview whose tikz source REQUIRES a `\usetikzlibrary`/macro that is present ONLY in the config-declared per-figure preamble template (and is NOT present in any other preamble the compile would otherwise use), that figure COMPILES successfully and renders as a real figure in the preview — proving the configured template, not a fixed built-in preamble, is what wraps the figure source.
  The per-figure template carries a single `<>` placeholder, and the figure source is substituted AT that placeholder before compile.
  Pointing config at a DIFFERENT template that LACKS that `\usetikzlibrary`/macro makes the SAME figure FAIL to compile — no figure is rendered in the preview — proving the compile outcome is governed by the now-configured template.
  The per-figure template is a config-declared, existing-file-validated path: a missing template path fails LOUDLY at config load — the app does not reach the editor with a silently-defaulted or empty template.
  Admissible because it fails if the figure COMPILES REGARDLESS of the configured template (a fixed preamble is still used, not the config template, so swapping to a template lacking the required library does not change the compile outcome — the figure renders either way), if the `<>` placeholder is NOT where the figure source lands (the figure source is substituted somewhere other than the placeholder, so the template does not actually wrap the source as authored), and if a MISSING template path is SILENTLY DEFAULTED (a built-in preamble is substituted) rather than producing a hard config-load failure.
  It is NOT satisfied by an assertion that a template file merely EXISTS on disk — a template that is present but never used to wrap the figure source would pass an existence check while the figure compiles identically with or without it (the fixed-preamble app this obligation must reject); the proof must observe that the figure compiles ONLY under the template carrying the required library and FAILS under a template lacking it.

- **P93 — [RETIRED 2026-06-20, ill-conceived: no source-line→rendered-SVG-element mapping exists — pdf2svg carries no per-node identity and procedural/loop tikz has no 1:1 correspondence. Scaffolding deleted.]** ~~invoking the source↔preview jump from the cursor on a specific node's source line selects/scrolls the preview to the MATCHING rendered element for THAT node, and editing the source then invoking re-sync updates the preview to the edited model.~~ With owned tikz source open in the editor whose figure is RENDERED in the live preview (the figure carries MULTIPLE distinct nodes), positioning the cursor on a SPECIFIC node's source line — the `\node …(<name>) at (<x>,<y>){<label>};` line for one chosen node — and invoking the jump (the TikzIt Ctrl+J jump-to-source action) SELECTS/SCROLLS the preview to the rendered element CORRESPONDING TO THAT node: the element the preview selects/scrolls to is the one for the node under the cursor, NOT a different node's element and NOT a no-op.
  Choosing a DIFFERENT node's source line and invoking the jump selects/scrolls to that other node's rendered element instead — the correspondence is per-node, so the jump target tracks which node line the cursor sits on (proving the jump resolves the specific source-line→rendered-element mapping, not a fixed scroll-to-one-place).
  Separately, EDITING the tikz source — a change that alters the model (e.g. moving a node's coordinate, renaming a node, adding/removing a node or edge) — and then invoking the re-sync (the TikzIt Ctrl+T re-parse action) UPDATES the preview to reflect the EDITED model: the rendered figure after re-sync matches the edited source, and the STALE pre-edit render does NOT persist.
  Admissible because it fails on a NO-OP jump (invoking the jump from a node's source line selects/scrolls NOTHING in the preview — no element is targeted), on a WRONG-ELEMENT jump (the jump selects/scrolls to a DIFFERENT node's rendered element than the one under the cursor, so the per-node correspondence is broken — the same wrong target regardless of which node line the cursor sits on, or a target that does not match the cursor's node), and on a STALE re-sync (after a source edit that changes the model, invoking the re-sync leaves the preview showing the pre-edit render — the edited model is not reflected).
  It is NOT satisfied by an assertion that a Ctrl+J / Ctrl+T KEYBINDING merely EXISTS — a test that would still pass on an app whose jump targets nothing, targets the wrong element, or whose re-sync leaves the preview stale is inadmissible; the proof must position the cursor on a SPECIFIC node's source line, invoke the jump, and observe the preview select/scroll to EXACTLY that node's rendered element (and a different node's line targeting that different node's element), AND edit the source + re-sync and observe the preview reflect the edited model rather than the stale render.

- **P94 — a tikz command present ONLY in the config-declared vendored tikz-command DB is surfaced by the insertion bar and the editor completion, and CHOOSING it inserts its declared insert body with the cursor at the declared offset.** With the editor open, a tikz command whose name/insert body is present ONLY in the config-declared vendored tikz-command DB (and is NOT a built-in or hardcoded scaffold) is SURFACED on BOTH the insertion bar's tikz surface AND the editor's autocomplete completion source — proving the bar palette and the CM6 completion are SEEDED from the DB, not from a hardcoded list.
  CHOOSING that command (from the insertion-bar surface or by accepting the completion) inserts its DECLARED INSERT BODY at the cursor — the multi-character insert text the DB declares for that command — NOT the bare command name as literal text, and the cursor lands at the command's DECLARED cursor OFFSET within that inserted body.
  The DB is a config-declared, load-validated source: pointing config at a DIFFERENT DB surfaces THAT DB's commands instead (the palette and completion are DATA-DRIVEN, not hardcoded — a command present only in the now-configured DB appears, and a command from the previously-configured DB no longer does).
  A MALFORMED or UNREADABLE DB is a HARD VISIBLE ERROR, never a silently-empty palette.
  Admissible because it fails on a HARDCODED or EMPTY palette that ignores the DB (the DB-only command is never offered on the bar or in the completion — the surfaces are not seeded from the DB), on LITERAL-NAME insertion instead of the insert body (choosing the command leaves the bare command name in the buffer rather than its declared multi-character insert body), on a WRONG cursor offset (the cursor does not land at the DB-declared offset within the inserted body), on an IGNORED DB (pointing config at a different DB leaves the SAME commands surfaced, proving a baked-in palette drives the surfaces rather than the configured DB), and on a SILENTLY-EMPTY palette for a malformed/unreadable DB (a malformed DB yields an empty palette with no visible error instead of a hard visible failure).
  It is NOT satisfied by an assertion that a DB file merely EXISTS on disk — a DB that is present but never read to seed the bar/completion would pass an existence check while the surfaces still offer their hardcoded scaffolds, which is exactly the ignored-DB app this obligation must reject; the proof must observe a DB-only command surfaced AND inserting its declared body at the declared offset, AND a different configured DB surfacing different commands.

- **P95 — an introduced tikz figure-compile error produces a clickable TikZ-mode log entry whose activation lands the editor cursor on the exact offending tikz source line.** With a tikz figure open in the live preview, introducing an error into that figure's tikz source — a MALFORMED tikz construct that makes the figure compile FAIL — produces a compile-log ENTRY in a TikZ-mode log tab/panel: the figure-compile diagnostic is surfaced to the user (the failure is VISIBLE in the app, not dropped to stderr only), parsed from the `file:line:message` LaTeX error the figure compile emits.
  ACTIVATING that log entry MOVES the editor cursor to EXACTLY the offending tikz SOURCE line — the line carrying the malformed construct — observed via the editor's cursor position (an independent read of the cursor's line, e.g. `cursorOffset()`/the goto-line surface), not merely a scroll.
  This is distinct from P11 (the pandoc compile log) and P74 (the pandoc-render structured-log jump): P95 is the FIGURE-compile log mapped back to the figure SOURCE line, the failure outcome of the same tikz→SVG compile seam P100 activates.
  Admissible because it fails on a MISSING log tab (the figure-compile error is invisible — it reaches only stderr, so no entry exists for the user to see or click — exactly the D-3 drop-and-log-to-stderr behaviour this obligation closes), on an UNPARSED error (a log entry exists but carries no parsed line target, so activating it is a no-op with no jump), and on a WRONG target line (activating the entry lands the cursor on a line OTHER than the one carrying the malformed construct).
  It is NOT satisfied by an assertion that a log panel merely EXISTS with no real error mapped — a test that would still pass on an app whose log tab is present but never receives the figure-compile diagnostic, or whose entry does not carry a real parsed line that the cursor lands on, is inadmissible; the proof must introduce a real malformed tikz construct, observe the resulting entry, ACTIVATE it, and read the editor cursor at exactly the offending source line.

- **P96 — a registered NON-tikz figure tracks its editable SOURCE alongside the included RENDER, and the edit action launches the diagram-tool editor on the SOURCE — never the render — with the pairing persisting across an app restart.** With a NON-tikz figure registered — a DUAL asset whose editable SOURCE (an `.ipe`/`.svg`) is tracked ALONGSIDE the included RENDER (the PDF/SVG actually embedded in the document) — invoking that figure's edit action launches the diagram-tool editor, via the plugin firewall, on the tracked editable SOURCE: the path the launched diagram-tool plugin receives is the SOURCE path, NOT the included render path.
  NO tikz extraction is attempted on a non-tikz figure (Ipe owns no tikz — there is no owned parser in this path).
  The source↔render pairing is held in a host-filesystem registry sidecar, so after an app RESTART the SAME figure still resolves to the SAME editable source: re-invoking the edit action after restart again launches the editor on the tracked source, the pairing intact.
  A missing tracked source is a LOUD error — never a silent fall-through to opening the render.
  Admissible because it fails if the edit action launches the editor on the included RENDER instead of the tracked editable SOURCE (the path the launched plugin receives is the render's, so the dual-asset tracking does not actually route the editor to the source), if TIKZ EXTRACTION is attempted on an Ipe figure (a non-tikz figure is wrongly treated as owned tikz rather than launched in its native editor on its native source), and if the source↔render PAIRING IS LOST after restart (re-invoking the edit action after an app restart no longer resolves the figure to its tracked source — the host-filesystem registry did not persist the pairing).
  It is NOT satisfied by an assertion that a registry struct merely EXISTS — a test that would still pass on an app that opens the render, attempts tikz extraction on an Ipe figure, or loses the pairing across restart is inadmissible; because a real Ipe/Inkscape GUI cannot be asserted headless, the WIRING is proven by a REAL diagram-tool plugin (exercising the REAL firewall launch path, not a mock) whose thin launch command RECORDS the argv path it received to a sentinel file, and the observable is that the recorded path is the tracked SOURCE — read by an INDEPENDENT process — both before and after an app restart.

- **P97 — selecting a subgraph and copying places EXACTLY that subgraph's canonical tikz on the system clipboard, re-parseable by the app's tikz parser.** With a tikz figure open in the editor whose owned tikz source carries MULTIPLE nodes and edges, SELECTING a subgraph — a region of that owned tikz source covering a PROPER SUBSET of its nodes/edges (not the whole picture) — and invoking the copy-subgraph action places onto the SYSTEM clipboard EXACTLY that subgraph rendered as deterministic CANONICAL tikz: the clipboard text, read by an INDEPENDENT process and RE-PARSED by the app's own tikz parser (the D-1 / P90 parser), yields a graph whose nodes and edges are EXACTLY those of the selected subgraph — the selected nodes/edges with their names, coordinates, props, and labels, and the edges whose BOTH endpoints lie in the selection — and NO node or edge outside the selection.
  The clipboard carries canonical tikz produced by the SAME `Graph::to_tikz()` serializer P90 round-trips, so the copied text re-parses STABLY back to the selected subgraph (not the verbatim selected characters, not a non-round-trippable serialization).
  A selection that is NOT parseable as tikz is a LOUD error — the copy action fails visibly and the clipboard is not populated with a raw-text guess — never a silent raw-text copy.
  Admissible because it fails on a WHOLE-GRAPH copy (the clipboard, re-parsed, carries nodes/edges OUTSIDE the selection — the whole picture was copied rather than the selected subgraph), on a RAW-TEXT / NON-TIKZ copy (the clipboard holds the raw selected characters verbatim rather than canonical tikz, so re-parsing by the app's parser does not recover the selected subgraph), on a NON-RE-PARSEABLE serialization (the clipboard text cannot be parsed back by the app's tikz parser, or parses to a different graph, so the round-trip from selection to clipboard to model breaks), and on a SILENT raw-text copy of an unparseable selection (an unparseable selection populates the clipboard with raw text instead of failing loudly).
  It is NOT satisfied by an assertion that a copy command merely EXISTS — a test that would still pass on an app that copies the whole graph, copies the raw selected text, or emits tikz its own parser cannot re-parse is inadmissible; the proof must read the system clipboard by an independent process and re-parse it into EXACTLY the selected subgraph.

- **P98 — an external rewrite of the currently-open owned figure reloads the in-app preview to reflect the new content, and a reload does NOT fire when the file is unchanged.** With an owned figure open in the live preview, when an INDEPENDENT process REWRITES that exact figure file on disk with NEW content (the external-edit return half of the launch→edit→return loop), the in-app preview RELOADS so that it reflects the NEW on-disk content — the rendered figure changes to match what the external process wrote, and the stale render does NOT persist.
  The change is detected via the EXACT P48 fingerprint (the FNV-1a content hash + nanosecond mtime `fsops.rs` captures on read/write): the reload fires precisely because the open owned figure's on-disk fingerprint DIVERGES from the fingerprint stored when it was opened/last loaded.
  A file that is NOT actually changed (its on-disk fingerprint is unchanged — same content, same mtime) triggers NO reload: an unchanged file does not produce a spurious reload.
  Admissible because it fails if the STALE render PERSISTS after the external write (no reload fires — the preview keeps showing the pre-rewrite figure even though the on-disk content changed, so the launch→edit→return loop is not closed), and if a SPURIOUS reload fires absent any real change (a reload is triggered when the open figure's fingerprint has NOT diverged — the file was not actually rewritten — proving the trigger is not the real fingerprint-divergence check P48 owns).
  It is NOT satisfied by an assertion that a file-watcher merely EXISTS — a test that would still pass on an app whose watcher is wired but never reloads the preview (the stale render persists), or that reloads on every tick regardless of content (spurious reloads), is inadmissible; the external rewrite must be performed by an INDEPENDENT process and the observable is the preview reflecting the NEW content after a real fingerprint divergence and NOT reloading when the fingerprint is unchanged.

- **P99 — registering and inserting an external-editor-produced SVG/PDF vector asset writes it into the CONFIGURED GLOBAL figures directory and inserts a markdown reference to that exact on-disk file at the cursor.** With an external-editor-produced vector asset to include — an Ipe/Inkscape-emitted SVG or PDF (not tikz) — invoking the insertion bar's vector-figure-inclusion action writes that asset as a real file into the CONFIGURED GLOBAL figures directory (`config.directories.figures` — the SAME ExistingDir P62 writes into, NEVER a project-local `./figures`) and inserts a markdown image reference (`![…](…)`) to that exact written file at the cursor.
  An independent process reading the configured global figures directory finds a newly-written vector asset whose bytes are the external asset's real bytes (a real, non-zero-length file is persisted — never a zero-length / unreadable asset), and the markdown reference inserted at the cursor points at that SAME on-disk file (the reference resolves to a file that exists — not a dangling reference).
  The inserted vector asset is REGISTERED in the D-7 / P96 dual-asset registry — the included render is tracked alongside its editable source — so the inserted figure is later re-openable in its source tool via P96's edit action.
  Admissible because it fails on a no-op insert (the inclusion action leaves the buffer unchanged so no reference appears at the cursor), on a dangling reference (a markdown reference is inserted but it points at a file that was never written / does not exist on disk), on a wrong-location write (the asset is written into a project-local `./figures` instead of the configured GLOBAL figures directory), on a zero-length/empty persist (the reference points at a file that exists but holds no bytes — nothing of the vector asset was actually persisted), and on an unregistered insert (the inserted asset is not recorded in the dual-asset registry, so the included figure has no tracked editable source and cannot be re-opened in its source tool).
  It is NOT satisfied by an assertion that an insert command merely EXISTS — a test that would still pass on an app that inserts a reference to a never-written file, writes into a project-local `./figures`, or persists a zero-length asset is inadmissible; the proof must read the configured GLOBAL figures directory and the editor buffer by an INDEPENDENT process and confirm the inserted reference names the exact non-zero-length on-disk asset.

- **P100 — a tikzpicture in the document renders as an inline vector figure in the live preview, and the tikz rendering toolchain is a hard doctor dependency.** With a document open in the live preview containing a tikz figure — a `tikzpicture` (or a tikz-tagged code block) — that figure is COMPILED and rendered as an inline VECTOR figure (an SVG) inside the live preview: a real rendered figure appears, and the raw tikz source is NOT shown verbatim in the preview.
  Additionally, the tikz rendering toolchain — the LaTeX compiler together with the PDF→SVG converter — is a HARD dependency surfaced by the startup doctor battery: when the converter is absent the doctor reports a LOUD FAIL at the startup gate, never a silent runtime skip of the figure.
  Admissible because it fails on an app that shows the tikz source as TEXT / does not produce an SVG in the preview (the compile seam is dormant — the figure is never compiled, so the raw source appears verbatim and no vector figure is rendered), and on an app that treats a MISSING tikz converter as a silent no-op (the figure is silently skipped or rendered as raw source with no doctor FAIL) instead of a loud doctor FAIL at the startup gate.
  It is NOT satisfied by an assertion that a filter/converter file merely EXISTS on disk — a required-on-disk filter that is never loaded produces no rendered SVG, so an existence check would pass on exactly the dormant app this obligation must reject; the proof must observe a real rendered SVG in the preview and a real doctor FAIL when the converter is absent.

- **P101 — workspace content search finds across files, with boolean operators and click-to-open-at-line.** In a multi-file project where `chapter1.md` contains `Minkowski bound` and `chapter2.md` contains `Minkowski lattice` and `Café`, open the Search view and query `Minkowski !lattice`: the results list `chapter1.md` (AND `Minkowski`, NOT `lattice`) and do NOT list `chapter2.md`; clicking the `chapter1.md` hit opens that file in the editor with the cursor on the matched line — an INDEPENDENT read of the editor cursor line equals the line containing `Minkowski bound`. The boolean grammar is space=AND, `|`=OR, `!term`=NOT, `"phrase"`=exact phrase; the search is over file CONTENT, not file names.
  Admissible because it fails on: a filename-only filter (content is never searched, so a content match is missed); a search that ignores `!`-negation (`chapter2.md` is wrongly listed despite containing `lattice`); and a result click that opens the file but NOT at the hit line (the independent cursor-line read does not equal the `Minkowski bound` line).
  It is NOT satisfied by an assertion that a Search view merely EXISTS — a view that lists nothing, or lists every file regardless of content, or opens files at line 1, would pass an existence check while failing every clause above; the proof must observe the negated file ABSENT from the results and the opened cursor ON the matched line.

- **P102 — per-directory restriction and relevancy heatmap.** In the multi-file project, restrict a search to a chosen subdirectory and query `Café`: only hits UNDER that subtree appear — a file matching `Café` OUTSIDE the chosen subtree is ABSENT from the results; and each result file carries a relevance weight rendered as a DISCRIMINABLE heat class — a file with more / exact matches shows a HIGHER-heat class than a single-match file (the two files are visibly distinguishable by heat class, not merely by list order).
  Admissible because it fails on: a restriction that searches the whole workspace anyway (the out-of-subtree matching file appears in the results); and a flat result list with no per-file relevance distinction (every hit shares one heat class regardless of match count, so the higher-match and single-match files are indistinguishable).
  It is NOT satisfied by an assertion that a scope control or a heat color merely EXISTS — a scope selector that does not actually constrain the results, or a uniform single-color list, would pass an existence check while failing both clauses; the proof must observe the out-of-subtree file ABSENT and two files in DIFFERENT heat classes by match count.

- **P103 — section / environment / math-zone motions move the cursor structurally, relative to its position.** In a buffer containing, in order, two headings, a `:::{.theorem}` fenced div, and two `$…$` math spans, with the cursor at the TOP of the buffer: invoking next-section moves the cursor to the SECOND heading line; invoking next-environment moves the cursor to the fenced-div line; invoking next-math-zone moves the cursor INSIDE the first math span; and invoking prev-section / prev-environment / prev-math-zone REVERSES each of those motions (each computed relative to the cursor's current position, landing on the nearest matching structure in the requested direction).
  The cursor position is read independently of the app's own report.
  Admissible because it fails on: a NO-OP motion (the cursor is unmoved after the motion is invoked); a motion that lands on the WRONG structure kind (next-environment landing on a heading line, or next-math-zone landing on a heading/fenced-div line, rather than the requested structure); and a motion that IGNORES the cursor position (always jumping to the FIRST or LAST matching structure regardless of where the cursor currently sits, so prev-* and next-* from a mid-buffer cursor do not move in opposite directions relative to it).
  It is NOT satisfied by an assertion that a motion command or keybinding merely EXISTS — a command that is wired but never moves the cursor, lands on the wrong structure kind, or jumps to a fixed first/last position regardless of the cursor would pass an existence check while failing every clause above; the proof must position the cursor, invoke each motion, and read the cursor landing on EXACTLY the requested structure in the requested direction relative to that cursor.

- **P104 — Ctrl+Shift+P runs a command via the firewall palette; Ctrl+P opens quick-open and opens the picked file.** Ctrl+Shift+P opens the COMMAND PALETTE — a firewall picker fed the app's command catalog — and SELECTING the "Fold All" command actually FOLDS the buffer: an INDEPENDENT observation of the editor's folded ranges shows the buffer folded after the selection, proving the palette RUNS the selected command, not merely lists it.
  Separately, Ctrl+P opens the QUICK-OPEN file finder — a firewall picker over the workspace files, NOT the command palette — and SELECTING a workspace file OPENS that file in the editor (the editor's active document becomes the selected file, read independently of the app's own report).
  Admissible because it fails on: Ctrl+P opening the COMMAND PALETTE (the un-fixed binding transposition — Ctrl+P must be quick-open, not the palette); a palette that LISTS commands but where running the selection is a NO-OP (the "Fold All" selection is offered but nothing folds — the folded-ranges observation shows the buffer unfolded); and a quick-open that LISTS files but whose SELECTION does NOT open the file (a file is offered but selecting it leaves the editor on the previous document, not the selected one).
  It is NOT satisfied by an assertion that a palette / quick-open surface merely EXISTS or merely LISTS its entries — a picker that surfaces commands/files but whose selection never folds the buffer / never opens the file would pass an existence-or-listing check while failing the run-the-selection clauses above.
  Because fzf is an interactive TUI undriveable headless, the WIRING is proven through the REAL plugin firewall by a REAL picker plugin (exercising the REAL firewall, not a mock in app logic) whose picker step is scripted to RETURN a deterministic selection (reading a fixture-configured selection / env and emitting the chosen command id / file path on stdout); the observable is then the buffer ACTUALLY FOLDING (the palette ran the command) and the selected file ACTUALLY OPENING (quick-open ran the selection) — not that the picker listed them.

- **P105 — surround/toggle edits transform the existing structure in place.** With the cursor inside a `:::{.theorem}` fenced div, invoking rename-environment to `lemma` makes the div's class become `.lemma` with the div BODY byte-unchanged — the EXISTING div is edited in place, NOT a new env inserted (only the one div remains, now `.lemma`). With the cursor on `\frac{a}{b}`, invoking toggle-fraction makes the buffer hold `a/b` at that position; invoking toggle-fraction AGAIN restores `\frac{a}{b}` at that position.
  With the cursor inside a `(x+y)` delimiter pair, invoking delete-delimiter-pair removes the parentheses, leaving `x+y` (the contents are kept; only the enclosing delimiters are removed).
  The resulting buffer contents are read independently of the app's own report.
  Admissible because it fails on: a rename that INSERTS A NEW ENV instead of editing the existing one (two envs appear — the original `.theorem` div plus a new `.lemma` one — rather than the single existing div's class changing in place); a fraction toggle that is ONE-WAY OR A NO-OP (toggling `\frac{a}{b}` does not produce `a/b`, or a second toggle does not restore `\frac{a}{b}`, or the toggle leaves the buffer unchanged); and a delimiter delete that REMOVES THE CONTENTS TOO (deleting the `(`…`)` pair around `x+y` leaves an empty result or drops `x+y` rather than leaving exactly `x+y`). It is NOT satisfied by an assertion that a rename / toggle / delete command merely EXISTS — a command that is wired but inserts a parallel env, toggles one-way or no-ops, or strips the delimited contents would pass an existence check while failing every clause above; the proof must position the cursor inside each existing structure, invoke the command, and read the buffer holding the in-place transform (one renamed div with its body intact, the round-tripped fraction, the contents-preserving delimiter removal).

- **P106 — the frontmatter editor round-trips ONLY the frontmatter block, leaving the body byte-for-byte unchanged, and inserts a well-formed block when none exists.** With a document open whose buffer begins with a `---` … `---` YAML frontmatter block carrying `title:` and `author:` followed by a body, opening the frontmatter editor modal and CHANGING the title and ADDING a `bibliography:` value, then confirming, makes the document's leading `---` block reflect EXACTLY the new fields — the frontmatter parses to the NEW title and the ADDED `bibliography:` value (the edited title is present and the previous title is gone; the bibliography value is present) — while the document BODY (every byte after the closing `---` of the frontmatter block) is BYTE-FOR-BYTE UNCHANGED from before the edit.
  Separately, with a document open that has NO frontmatter block (the buffer begins directly with body content), opening the frontmatter editor, setting a field, and confirming makes the buffer GAIN a well-formed leading `---` … `---` block carrying that field, ahead of the original body — and the original body content is preserved below the inserted block.
  The frontmatter block and the document body are read independently of the app's own report (an independent read of the buffer / on-disk file).
  Admissible because it fails on: an editor that REWRITES THE WHOLE DOCUMENT (the body bytes change after confirm — re-emitting or reflowing the body, not just the frontmatter block, so the byte-for-byte body equality breaks); a field edit that is DROPPED (after confirm the frontmatter does not carry the new title or the added `bibliography:` value — the form's edits never reached the block); and a missing-frontmatter case that FAILS TO INSERT A BLOCK (a document with no frontmatter gains no `---` block on confirm, or gains a malformed one that does not parse as a `---`-delimited YAML block).
  It is NOT satisfied by an assertion that a frontmatter modal merely EXISTS or merely OPENS — a modal that renders fields but whose confirm rewrites the whole buffer, drops the edited fields, or never inserts a block for a frontmatter-less document would pass an existence-or-opens check while failing every clause above; the proof must change a field AND add a field, confirm, and read the frontmatter block carrying EXACTLY those new fields with the body bytes unchanged, plus the frontmatter-less document gaining a well-formed block on confirm.

### Milestone — Phase F: PDF preview (P107–P111 + P113 ratified; F1 = P107 live PDF preview; F4 = P110 compile toggle + P111 inline warnings; F6 = P113 slides fast-feedback preview; F5/P112 STRUCK per the `-f markdown`/`sourcepos` ruling)

P107 (added 2026-06-19) covers the Phase F headline sub-milestone F1 "embedded pdf.js viewer + compile-on-idle scheduler", authored from [[phase-f-pdf-preview-gummi.md]] (the F1 section + the drafted P107) and ratified by the 2026-06-19 controller decision ("execute all phases, no stops"; P107–P113 as drafted, no renumber — ceiling was P106 from Phase E; Phase F specs continue p116+). It is the live PDF-preview surface a one-shot export (P8) does not give: an embedded **pdf.js** viewer pane (vendored `pdfjs-dist`, app-owned UI — NOT a plugin; its worker/cmaps ship as offline app resources, the MathJax local-asset precedent) fed by a **compile-on-idle scheduler** — a debounce sibling of the existing HTML `scheduleRender`/`doRender` loop, with its own latest-wins sequence guard and its own `RenderStatus` cluster — that drives the configured `[export.<id>]`-shaped PDF compile command (today `[export.pdf]` = pandoc → lualatex) through the existing `export_sync`/`exportToPath` boundary to a real `.pdf` on disk, then loads that PDF into the viewer via `convertFileSrc`. The app core grows a VIEWER and a SCHEDULER only — it never learns what lualatex/latexmk are; the compile stays a configured command, NOT a hardcoded recipe (the total-externality discipline B/C established for the renderer).
It REUSES P8's PDF-on-disk discipline verbatim: the proof reads the produced PDF off disk through an INDEPENDENT process (`pdfinfo` for validity/metadata, `pdftotext` for witness text) so the obligation cannot be satisfied by a faked/blank viewer, a stale artifact, or an unwired compile — and additionally reads the live viewer DOM for a rendered page canvas (pdf.js produced pixels), which P8 (a headless export) does not assert.
It states the OBSERVABLE behaviour ONLY — no scheduler-debounce, sequence-guard, viewer-component, worker-asset, or `convertFileSrc` wiring internals (the wiring belongs to the implementer, not this obligation).
It is proven through the real app on a real display, with the produced PDF read off disk by independent processes and the viewer's rendered-canvas read from the live preview DOM, independently of the app's own report.
It is admissible only if it FAILS on a plausibly broken app.
F5/P112 (bidirectional SyncTeX click-jump) is STRUCK — not merely gated — by the standing ruling that `-f markdown` is permanent and precise-`sourcepos` features are dropped from scope; P112 is NOT written.
P108 (added 2026-06-19) covers the Phase F sub-milestone F2 "temp-directory build isolation", authored from [[phase-f-pdf-preview-gummi.md]] (the F2 section + the drafted P108) and ratified by the same 2026-06-19 controller decision.
It routes the LaTeX build into an isolated temp/build directory so the driver's intermediates (`.aux`/`.log`/`.fls`/`.out`/build `.pdf`) never scatter beside the user's thesis: today `run_export` spawns the export-plugin command with `current_dir` = the SOURCE FILE PARENT directory (`plugins.rs`, the `current_dir(&dir)` line where `dir` is the source's parent), so latexmk/lualatex intermediates land in the source tree.
F2 supplies the build path via a validated placeholder (the `{builddir}`/`{outdir}` family validated alongside the existing `{file}`/`{output}` placeholders); latexmk's own `-output-directory`/`-jobname` native build-isolation (the mechanism Gummi uses, routing `-jobname` to a temp dir) is the PLUGIN's argv concern — the app supplies the path, the driver flags do the isolation (no app-side aux-file-shuffling scheme).
It KEEPS P8 green: the one-shot export `{output}` artifact still lands at the user-chosen path with its witnesses + engine discrimination unchanged — only the intermediates move.
It REUSES P8's PDF-on-disk + independent-process discipline for the artifact half and adds an independent before/after directory listing for the no-litter half.
It states the OBSERVABLE behaviour ONLY — no temp-dir-resolution, placeholder-substitution, or `current_dir` wiring internals (the wiring belongs to the implementer, not this obligation).
It is admissible only if it FAILS on a plausibly broken app.
P109 (added 2026-06-19) covers the Phase F sub-milestone F3 "multi-pass (latexmk) auto-orchestration as a configured driver", authored from [[phase-f-pdf-preview-gummi.md]] (the F3 section + the drafted P109) and ratified by the same 2026-06-19 controller decision.
It is the "references done right" substance: the configured PDF command is a SHIPPED, vendored `latexmk` driver whose argv IS the real `/usr/bin/latexmk` binary (pandoc md→latex, then `latexmk -lualatex -output-directory={builddir}` over the emitted `.tex`), promoted to a real vendored plugin in `src-tauri/resources/vendor/plugins/` on the export-plugin contract (the `pandoc-pdf-export` sibling shape) with a `[[doctor_checks]]` confirming `latexmk` resolves to an executable (the `pdf-export-executable`/`check-executable.sh` argv[0]-resolves-and-`--version`-exits-0 precedent).
RESEARCH-FIRST (HARD RULE #0): the REAL latexmk binary ALREADY runs exactly-as-many-passes-as-needed and auto-invokes BibTeX (latexmk's default) — the app/plugin does NOT orchestrate passes itself; the deliverable is the configured driver command + the doctor check, not a bespoke pass loop.
The compile boundary is UNCHANGED from F1/F2 (the same `[export.<id>]`-shaped configured command through the existing `export_sync`/`run_export` boundary, the F2 `{builddir}` isolation intact); F3 only swaps the configured command to the latexmk multi-pass driver.
`bibtex` + `latexmk` are confirmed present (`/usr/bin/latexmk`, `/usr/bin/bibtex`); a missing `latexmk` is a LOUD doctor FAIL, never a fallback or soft default (single-user pre-launch Linux dev software).
It REUSES P8's PDF-on-disk + independent-process discipline: the produced PDF is read off disk by an INDEPENDENT `pdftotext` for the rendered-citation + resolved-cross-reference witnesses.
It states the OBSERVABLE behaviour ONLY — no pass-count, BibTeX-invocation-order, or `-output-directory` wiring internals (the wiring belongs to the implementer, not this obligation).
It is admissible only if it FAILS on a plausibly broken app.
P110 + P111 (added 2026-06-19) cover the Phase F sub-milestone F4 "auto/manual + fast/full compile controls; inline warning surfacing", authored from [[phase-f-pdf-preview-gummi.md]] (the F4 section + the drafted P110/P111) and ratified by the same 2026-06-19 controller decision.
RESEARCH-FIRST (HARD RULE #0): both controls are config-persisted state SELECTING BETWEEN CONFIGURED commands — NOT new build machinery.
The auto/manual + fast/full toggles live in the `[preview]` config table next to `debounce_ms` (the F1 pdf-preview fields), enumerating two configured PDF command ids — FAST = a draft single-pass command, FULL = the F3 `latexmk` multi-pass driver (P109) — fail-loud validated in the static config (no runtime defaults, no soft fallbacks).
The auto/manual state GATES whether the F1 compile-on-idle scheduler in `App.svelte` fires; a MANUAL "Recompile PDF" command lands via `buildCommands` (the Overleaf Recompile-button analog).
The fast/full state selects WHICH configured command the scheduler/recompile runs.
The inline-warning surfacing LEVERAGES an EXISTING latex-log parser (the pplatex / TexLogParser-class layer Phase A's structured-log work names — P74) over `render.rs::format_log` / the compile log — it does NOT write a bespoke log parser.
IMPORTANT SCOPE: the markdown-source-line JUMP for warnings is STRUCK (F5/sourcepos dropped per the `-f markdown` ruling); P111's floor is surfacing the warning + its LATEX-LOG line as a structured entry — NO markdown-line jump.
Single-user pre-launch Linux dev software: no fallbacks, no soft defaults; fail loud — a clean compile NEVER fabricates a phantom warning, and a failed compile NEVER shows a stale PDF. Both state the OBSERVABLE behaviour ONLY — no scheduler-gating, config-field, command-id-selection, or log-parser wiring internals (the wiring belongs to the implementer, not these obligations).
Both are admissible only if they FAIL on a plausibly broken app.
P113 (added 2026-06-19) covers the Phase F sub-milestone F6 "slides (beamer/revealjs) fast-feedback preview", authored from [[phase-f-pdf-preview-gummi.md]] (the F6 section + the drafted P113) and ratified by the same 2026-06-19 controller decision.
RESEARCH-FIRST (HARD RULE #0): the slide deck is produced by the REAL pandoc reveal.js writer (`pandoc -t revealjs`, slides are "just a different pandoc command") rendered by the maintained reveal.js library pandoc already targets — there is NO owned slide renderer; the slides path is a separate renderer/exporter plugin (a `revealjs` renderer plugin = `pandoc -t revealjs+embed_resources`, sibling to the existing `pandoc-renderer` plugin in `src-tauri/resources/vendor/plugins/`). F6 only WIRES that existing renderer plugin into the SAME compile-on-idle scheduler F1 built (P107): editing re-renders the reveal.js deck on idle INTO THE PREVIEW (reveal.js HTML into the existing `PreviewPane.svelte` iframe — the FAST HTML path, distinct from a full beamer→PDF compile), so the deliverable is the fast-feedback loop, NOT a slide authoring UI and NOT a new slide renderer.
It is INDEPENDENT of F1–F5 (it depends only on F1's scheduler abstraction).
It states the OBSERVABLE behaviour ONLY — no scheduler-debounce, plugin-discovery, `pandoc -t revealjs` argv, or iframe-wiring internals (the wiring belongs to the implementer, not this obligation).
It is proven through the real app on a real display, with the re-rendered deck read from the live preview DOM independently of the app's own report.
It is admissible only if it FAILS on a plausibly broken app.

- **P107 — live PDF preview renders a real compiled PDF in the embedded pdf.js viewer.** With the PDF preview pane active and the configured PDF compile command set (the shipped `[export.pdf]` = pandoc → lualatex), open `demo.md`; after the compile-on-idle debounce elapses a PDF is produced ON DISK and shown in the embedded pdf.js viewer.
  An INDEPENDENT process reads the produced PDF file off disk: it is a VALID PDF (the `%PDF-` magic header, and `pdfinfo` parses a `Pages:` count) whose `pdftotext`-extracted text contains BOTH witnesses "Geometry of Numbers" and "Minkowski bound" (so the PDF is the freshly-compiled `demo.md`, not a stale or unrelated artifact), AND the viewer DOM shows a RENDERED PAGE CANVAS — pdf.js produced actual pixels for a page, not an empty pane.
  On a nonzero compile exit the status cluster shows failed-compile and the log pane carries the command + stderr + exit status (the P11 shape); the viewer NEVER shows a stale PDF as if fresh.
  Admissible because it fails on: a BLANK / FAKED viewer (no rendered page canvas in the DOM — the pane is empty or shows a placeholder, so pdf.js never painted a page); a STALE artifact (the PDF on disk predates the edit or lacks the witnesses — `pdftotext` does not recover "Geometry of Numbers" / "Minkowski bound"); an UNWIRED compile (the compile-on-idle scheduler never fires, so NO PDF file is produced on disk after the debounce); and a viewer fed a NON-PDF blob (the bytes loaded into the viewer are not a valid PDF — `pdfinfo` REFUSES them / no `%PDF-` magic / no `Pages:`). It is NOT satisfied by an assertion that a PDF viewer pane or a `compilePdf`/scheduler symbol merely EXISTS — a pane that is mounted but never fed a compiled PDF, a scheduler that is wired but never produces an on-disk artifact, or a viewer that loads a non-PDF without complaint would pass an existence check while failing every clause above; the proof must read the produced PDF off disk by an INDEPENDENT process (`pdfinfo` validity + `Pages:`, `pdftotext` carrying BOTH witnesses) AND read the viewer DOM for a rendered page canvas, after the compile-on-idle debounce, with the failed-compile path surfacing the command/stderr/exit rather than a stale PDF.

- **P108 — PDF build intermediates do not litter the source tree; the user-chosen export artifact still lands at its chosen path.** With a project whose source file lives in directory `D`, compiling that project to PDF (the configured `[export.<id>]`-shaped PDF command — today `[export.pdf]` = pandoc → lualatex via latexmk) produces NO new LaTeX intermediate files in `D`: an INDEPENDENT process listing `D` before and after the compile finds no newly-created `*.aux`, `*.log`, `*.fls`, `*.out`, or build `*.pdf` in `D` — the intermediates live in the isolated build/temp directory the app supplies (via the driver's own `-output-directory`/`-jobname` build isolation), NOT beside the thesis source.
  Simultaneously, the user-chosen ONE-SHOT export (P8) is unaffected: exporting to a chosen `{output}` path still leaves a valid PDF at EXACTLY that chosen path (read off disk by an INDEPENDENT process — the P8 witnesses "Geometry of Numbers" + "Minkowski bound" present, the engine still discriminated).
  The isolation moves the INTERMEDIATES only; it never relocates or drops the user-requested artifact.
  Admissible because it fails on: the current `current_dir = source parent` behavior where the driver scatters its `.aux`/`.log`/`.fls`/`.out`/build-`.pdf` into `D` (the independent before/after listing of `D` shows newly-created intermediate files); an isolation that ALSO misplaces the one-shot export artifact (the P8 `{output}` PDF is missing from its chosen path, or no longer carries the witnesses / engine discrimination — the intermediate-isolation work broke the user-chosen output P8 guarantees); and an unresolvable build/temp directory treated SILENTLY (the compile proceeds writing intermediates into `D` anyway, or produces no artifact, instead of failing LOUDLY on the missing build dir).
  It is NOT satisfied by an assertion that a `{builddir}`/temp-dir symbol or an `-output-directory` flag merely EXISTS in the configured command — a command that names a build dir but whose driver still writes intermediates beside the source, or whose isolation drops the user's `{output}`, would pass an existence check while failing the clauses above; the proof must list `D` by an INDEPENDENT process before and after the compile and observe NO new intermediate files created in `D`, while the P8 export PDF still appears at its chosen path with its witnesses and engine discrimination intact.

- **P109 — the configured multi-pass latexmk driver fully resolves references that a single pass leaves unresolved.** With the configured PDF compile command set to the vendored `latexmk` multi-pass driver (pandoc md→latex, then `latexmk -lualatex -output-directory={builddir}` over the emitted `.tex`), compile a fixture that contains BOTH a `\cite` (with a config-declared bibliography) AND a forward cross-reference (a `\ref` to a `\label` that appears LATER in the document) — a fixture engineered so that a SINGLE LaTeX pass leaves BOTH unresolved.
  An INDEPENDENT process (`pdftotext`) reads the produced PDF off disk and recovers, in the extracted text, BOTH: the CITATION RENDERED to its author/year (the real bibliography entry, NOT a raw `[?]` / unresolved-citation marker), AND the cross-reference RESOLVED to a real number (NOT `??`). The presence of the rendered citation PROVES BibTeX ran (latexmk auto-invoked it); the resolved forward reference PROVES more than one LaTeX pass ran (the `.aux` from pass N fed pass N+1). The app/plugin orchestrates NO passes itself — the real `latexmk` binary (its argv[0], confirmed by the `[[doctor_checks]]` that `latexmk` resolves) runs exactly-as-many-passes-as-needed and auto-invokes BibTeX by its own default behaviour.
  Admissible because it fails on: a SINGLE-PASS command (the cross-reference stays `??` and/or the citation stays an unresolved marker in the extracted text — one pass cannot resolve a forward reference); and a driver that NEVER invokes BibTeX (the citation stays UNRENDERED — no author/year, the `[?]`/unresolved-citation marker survives — because the `.bbl` was never built).
  It is NOT satisfied by an assertion that a `latexmk` plugin or an `-output-directory`/multi-pass symbol merely EXISTS, nor by a doctor check that `latexmk` resolves alone — a configured command that resolves to latexmk but is fed a fixture with nothing to resolve, or a proof that asserts only the driver's presence, would pass an existence check while proving NOTHING about multi-pass + BibTeX; the proof must compile the single-pass-unresolvable `\cite` + forward-`\ref` fixture and read the produced PDF off disk by an INDEPENDENT `pdftotext`, observing the citation RENDERED to author/year and the cross-reference RESOLVED to a real number (no `[?]`, no `??`).

- **P110 — the auto/manual + fast/full PDF-compile controls are honored: manual suppresses idle recompiles until an explicit Recompile, and fast/full select different configured commands with different artifacts.** The PDF preview pane carries two config-persisted controls (in `[preview]` next to `debounce_ms`): an auto/manual toggle and a fast/full selection, each choosing among the configured PDF command ids (FAST = the draft single-pass command, FULL = the F3 `latexmk` multi-pass driver).
  (a) MANUAL — with the PDF preview set to MANUAL, editing the buffer does NOT trigger a PDF recompile: an INDEPENDENT process reading the on-disk PDF finds it BYTE-UNCHANGED after the compile-on-idle debounce window has elapsed (the F1 scheduler did not fire), and NO new build runs.
  Invoking the explicit "Recompile PDF" command (the `buildCommands` palette entry) THEN produces a fresh PDF on disk — an INDEPENDENT `pdftotext` recovers the just-edited witness text the pre-recompile PDF lacked.
  (b) AUTO — with the PDF preview set to AUTO, editing the buffer DOES trigger a recompile within the debounce window: after the debounce an INDEPENDENT process reads a freshly-compiled on-disk PDF whose `pdftotext` text carries the edit, with no explicit Recompile command invoked.
  (c) FAST→FULL — switching the selection from FAST to FULL changes WHICH configured command runs, observable BOTH in the compile log (the logged command line differs — a different argv[0]/flags between the single-pass draft and the `latexmk` multi-pass driver, the P11 raw-log surface) AND in the produced artifact (the FULL artifact resolves references P109-style — a `\cite` rendered to author/year and a forward `\ref` resolved to a real number, recovered by an INDEPENDENT `pdftotext`; the FAST artifact does NOT — it leaves the citation/cross-reference unresolved).
  Admissible because it fails on: an IGNORED auto/manual toggle (MANUAL still auto-recompiles — the on-disk PDF changes within the debounce window despite no Recompile command, so the manual gate is dead); a Recompile command that is a no-op (after MANUAL + Recompile no fresh PDF carrying the edit appears on disk); an IGNORED fast/full selection (FAST and FULL run the SAME command — the logged command line is identical and both artifacts resolve, or neither resolves, the references, so the selection does not pick the command); and an AUTO mode that never fires (no freshly-compiled PDF appears after the debounce).
  It is NOT satisfied by an assertion that an auto/manual or fast/full toggle symbol merely EXISTS in config or the UI — a toggle whose state the scheduler ignores, or a fast/full selector that runs one hardcoded command regardless, would pass an existence check while failing every clause above; the proof must observe the on-disk PDF (BYTE-UNCHANGED after the debounce under MANUAL; freshly carrying the edit after Recompile and under AUTO) read by an INDEPENDENT process, AND the logged command line differing between FAST and FULL, AND the FULL-vs-FAST artifact difference (FULL resolves the `\cite`/`\ref`, FAST does not) read by an INDEPENDENT `pdftotext`.

- **P111 — a real LaTeX/pandoc WARNING is surfaced as a structured Problems entry distinct from a hard error and from the raw log dump; a clean compile surfaces none.** A PDF compile of a fixture ENGINEERED to emit a LaTeX/pandoc WARNING but NOT an error (e.g. an undefined-reference or overfull-`\hbox` warning) surfaces that warning, parsed by the existing latex-log parser (pplatex/TexLogParser-class — the P74 structured-log layer) from the real compile log, as a STRUCTURED entry in the compile/problems pane carrying `{severity = warning, the warning message text, the latex-log line}` — the structured entry exists IN ADDITION to (not instead of) the raw log (the P11 surface stays present and unchanged).
  The structured warning entry is DISTINCT from a hard error (its severity is `warning`, not `error`) AND DISTINCT from the raw log dump (it is a parsed `{severity, message, latex-log line}` record, not a slice of unparsed log text).
  A CLEAN compile (a fixture that emits NO warning) surfaces NO warning entry — the Problems pane carries no `severity = warning` record (no phantom/fabricated warning).
  NOTE: the markdown-source-line jump is OUT OF SCOPE (F5/sourcepos STRUCK) — P111's floor is the LATEX-LOG line carried on the entry, NOT a markdown-source-line jump.
  Admissible because it fails on: a path that surfaces ONLY errors (the warning is DROPPED — no `severity = warning` entry exists for a compile whose real subprocess output contains the warning); a path that shows warnings ONLY as raw log text with no structured severity/line (no parsed `{severity, message, latex-log line}` record exists — only the P11 raw dump, so there is nothing to distinguish a warning from an error or from prose log text); and a path that FABRICATES a phantom warning on a clean compile (a `severity = warning` entry appears even though the real subprocess output carries no warning).
  It is NOT satisfied by an assertion that a Problems pane or a warning-parser symbol merely EXISTS — a pane that is mounted but never populated from the real log, or a parser wired but whose output is never surfaced as a distinct structured entry, would pass an existence check while failing the clauses above; the proof must compile the warning-emitting fixture, observe a structured `{severity = warning, message text, latex-log line}` entry parsed FROM the real compile log and distinct from both a hard error and the raw P11 dump, AND compile a clean fixture and observe NO such entry (no phantom warning).

- **P113 — the slides renderer plugin re-renders a reveal.js deck into the preview on idle, carrying the edited witness text in real reveal.js slide structure.** With the slides renderer plugin active (a `revealjs` renderer plugin whose command is the real `pandoc -t revealjs+embed_resources`, sibling to the `pandoc-renderer` plugin), open `demo.md` containing a slide-separator structure (a multi-slide deck — the `---`/heading-level slide breaks pandoc's revealjs writer splits on), then EDIT the buffer (append a discriminating witness sentence into one slide); within the compile-on-idle debounce window the F1 scheduler re-renders the deck into the preview, and the preview DOM contains the REVEAL.JS SLIDE STRUCTURE — the `.reveal` container and the per-slide `section` elements the revealjs writer emits — with one `section` carrying the EDITED witness text.
  The re-render rides the SAME compile-on-idle scheduler F1 built (P107) on the FAST HTML path (reveal.js HTML into the `PreviewPane.svelte` iframe), distinct from a full beamer→PDF compile.
  Admissible because it fails on: a slides path that does NOT re-render on idle (after the debounce the edited witness sentence is ABSENT from the deck — the scheduler never re-ran the slides command, so the preview shows the pre-edit deck); a plugin that emits ORDINARY single-document HTML instead of a reveal.js deck (the preview DOM carries no `.reveal` container and no per-slide `section` slide structure — it is a flat document, not a deck, so the revealjs writer was not the command that ran); and an UNWIRED slides command (the preview is EMPTY — no deck is produced at all because the slides renderer plugin is not actually driven by the scheduler).
  It is NOT satisfied by an assertion that a revealjs plugin or a slides-preview symbol merely EXISTS — a plugin that is discovered but never driven by the compile-on-idle scheduler, or a preview that renders flat HTML without slide structure, would pass an existence check while failing every clause above; the proof must EDIT the buffer, wait the debounce, and read the live preview DOM for the reveal.js slide structure (`.reveal` / per-slide `section`) carrying the edited witness text — independently of the app's own report.

- **P114 — Flattened self-contained arXiv bundle compiles with no system styles.** Export the witness project to an arXiv bundle (Phase G / G1, [[phase-g-arxiv-export.md]] "### G1"). The arXiv target is a vendored `arxiv-export` FIREWALL plugin — an orchestrating script sibling to the `pandoc-pdf-export`/`pandoc-html-export` plugins under `src-tauri/resources/vendor/plugins/`, riding the SAME export firewall (`run_plugin`/`configure_plugin` in `plugins.rs`) the shipped export plugins use; the app core is unchanged (the whole md→tex→flatten→materialize→tar pipeline IS the plugin's script/argv).
  G1 emits the `.tex` via `pandoc … -t latex` (the app's owned renderer), FLATTENS every `\input`/`\include` into ONE root `.tex` following the canonical `latexpand` resolution algorithm (the established LaTeX flattener — `/usr/bin/latexpand`, leveraged/cited, NOT a greenfield flattener), and MATERIALIZES the dependent macros/`.sty` into the bundle dir by COPYING the REAL `.sty`/macro files the `research_draft.tex`/`dzg-unified` preamble pulls in (`src-tauri/resources/vendor/pandoc-config/styles/` — no hand-rolled macro extraction), then tars the bundle to `{output}` as a `.tar.gz`. The externally-observable state: an INDEPENDENT process unpacks the tarball into a fresh directory with NO access to the user's texmf/system style files (compile with `TEXMFHOME` pointed at an EMPTY dir) and runs `latexmk`/`pdflatex` from the bundle ROOT; a valid PDF is produced (valid per `pdfinfo`) whose extracted text (`pdftotext`) contains the witnesses "Geometry of Numbers" AND "Minkowski bound".
  Additionally: the bundle's root `.tex` contains NO unresolved `\input`/`\include`, and every custom `.sty`/macro the document uses is present as a REAL file in the bundle (read off disk by the independent process, never the app's report).
  Admissible because it FAILS on a bundle that left `\input`/`\include` unresolved (the root `.tex` still references missing section files, so the no-system-styles compile errors on the missing files), and on a bundle that did NOT materialize the macros (the compile errors on undefined macros — e.g. `\RR`/`\coxeterblacknode` from the preamble's macro tiers — precisely BECAUSE the system styles are absent under the empty `TEXMFHOME`). It is NOT satisfied by an assertion that the `arxiv-export` plugin, a flatten symbol, or a tarball merely EXISTS, nor by a bundle that compiles only because the host texmf silently supplied the macros — a plugin that ran but emitted a bundle with dangling `\input`s, or one whose compile leans on the system styles, would pass an existence check while failing the clauses above; the proof must compile the unpacked bundle from its ROOT with `TEXMFHOME` pointed at an EMPTY dir and observe the witness PDF, AND read the root `.tex` for the absence of unresolved includes and the bundle dir for the presence of the real materialized `.sty`/macro files — all by INDEPENDENT processes.

- **P115 — `.bbl` baked into the bundle, named to the main `.tex`, with NO `.bib` present.** Export the witness project to an arXiv bundle (Phase G / G2, [[phase-g-arxiv-export.md]] "### G2") for a witness project carrying at least one `\cite` against a config-declared bibliography (a real `.bib`, e.g. the `tests/proof/fixtures/references.bib`-class witness).
  The arXiv target is the SAME vendored `arxiv-export` FIREWALL plugin as P114 — its `export.sh` orchestrating script, extended: after G1's flatten + macro-materialization produce the self-contained bundle, the plugin runs the REAL `/usr/bin/latexmk` binary (a hard dep, the P109/F3 latexmk multi-pass driver is the reference invocation) `-pdf` INSIDE the flattened bundle, so latexmk's OWN multi-pass BibTeX/biber build resolves the citations and PRODUCES the intermediate `.bbl`; the plugin then CAPTURES latexmk's own `.bbl` into the bundle RENAMED to the main `.tex`'s basename (`main.tex` → `main.bbl`) and DELETES the `.bib` from the bundle.
  The plugin owns NO bibliography processing — it leverages latexmk's own intermediate `.bbl`; the `.bib` is removed because arXiv reads the `.bbl` directly and a leftover required `.bib` BLOCKS submission.
  The whole md→tex→flatten→materialize→latexmk-bake-`.bbl`→delete-`.bib`→tar pipeline IS the plugin's script; the app core is unchanged (the same export firewall `run_plugin`/`configure_plugin` in `plugins.rs`). The externally-observable state: an INDEPENDENT process unpacking the emitted tarball finds a `<main>.bbl` file whose BASENAME EQUALS the bundle's main `.tex` basename (`main.tex` ⇒ `main.bbl`, the only name arXiv reads), whose CONTENTS include the cited entry's formatted reference (the bibliography text the `\cite` resolves to — read off disk, e.g. the witness author/title string from the real `.bib` entry, proving the citation was actually resolved into the `.bbl`), AND finds NO `.bib` file ANYWHERE in the bundle (an independent `find … -name '*.bib'` over the unpacked tree returns empty).
  All assertions are by INDEPENDENT processes (`tar`, `find`, byte/string reads) on the REAL emitted tarball, never the app's report.
  Admissible because it FAILS on a bundle that shipped the `.bib` instead of the `.bbl` (a `.bib` is present in the unpacked tree — exactly arXiv's blocking case, the citation never baked), on a `.bbl` named DIFFERENTLY from the main `.tex` (e.g. left at latexmk's jobname or some other stem so its basename ≠ the main tex basename — arXiv would not read it), and on an empty/absent `.bbl` (the `.bbl` is missing, or present but does NOT contain the cited entry's formatted reference — the citation was never resolved into the bundle).
  It is NOT satisfied by an assertion that latexmk ran, that a `.bbl` symbol/path exists, or that the plugin emitted a tarball — a bundle that still carries the `.bib`, one whose `.bbl` is misnamed, or one whose `.bbl` is empty of the cited reference would each pass a mere existence/ran check while failing the clauses above; the proof must unpack the REAL tarball and, by INDEPENDENT processes, (a) read the `.bbl` basename and confirm it EQUALS the main `.tex` basename, (b) read the `.bbl` contents and confirm the cited entry's formatted reference is present, and (c) `find` the whole tree and confirm NO `.bib` survives.

- **P116 — TikZ externalized to a precompiled PDF figure, with NO tikz source remaining in the bundle.** Export the witness project containing a `tikzcd`/`tikzpicture` diagram to an arXiv bundle (Phase G / G3, [[phase-g-arxiv-export.md]] "### G3"). The arXiv target is the SAME vendored `arxiv-export` FIREWALL plugin as P114/P115 — its `export.sh` orchestrating script, extended: after G1's flatten + macro-materialization produce the self-contained bundle and BEFORE G2's `/usr/bin/latexmk` `.bbl` bake, the plugin PRECOMPILES every `tikzpicture`/`tikzcd` in the emitted `.tex` to a PDF figure written INTO the bundle and REWRITES the `.tex` so each diagram becomes a `\includegraphics{<fig>.pdf}` referencing that PDF. The compile is DRIVEN by the EXISTING `tikzcd.lua` filter's `pdflatex` compile core (`run_pdflatex_and_convert`, `src-tauri/resources/vendor/pandoc-config/filters/tikzcd.lua`, which already `pdflatex`-compiles `tikzpicture`/`tikzcd` to a PDF via the `standalone-tikz.tex` per-figure template, `src-tauri/resources/vendor/pandoc-config/templates/standalone-tikz.tex`) — the plugin OWNS NO new tikz compiler, it leverages that filter's existing `pdflatex`→PDF output and emits the PDF into the bundle instead of an SVG into the preview.
  The bundle then ships NO tikz toolchain dependency to arXiv (in-browser TikZJax for the bundle is a BANNED non-goal — [[parity-research/arxiv-export]] "Dispositions"; the figures are precompiled via the existing filter toolchain).
  The whole md→tex→flatten→materialize→tikz-externalize→latexmk-bake-`.bbl`→tar pipeline IS the plugin's script; the app core is unchanged (the same export firewall `run_plugin`/`configure_plugin` in `plugins.rs`). The externally-observable state: an INDEPENDENT process unpacking the emitted tarball finds (a) a REAL precompiled figure file in the bundle — a PDF, NON-ZERO in size, a VALID PDF per `pdfinfo`; (b) the bundle's root `.tex` REFERENCES that figure via a `\includegraphics` whose target is that on-disk PDF file (the reference resolves to a file that was actually written); and (c) NO `\begin{tikzpicture}` and NO `tikzcd` source remaining in ANY `.tex` of the bundle (an independent grep over every `.tex` in the unpacked tree finds no `\begin{tikzpicture}`/`\begin{tikzcd}` environment).
  All assertions are by INDEPENDENT processes (`tar`, `find`, `pdfinfo`, `file`, grep, byte reads) on the REAL emitted tarball, never the app's report.
  Admissible because it FAILS on a bundle that left the tikz source INLINE (the `tikzpicture`/`tikzcd` environment is STILL present in a bundled `.tex` — the bundle would ship the tikz toolchain dependency to arXiv, which does no tikz compilation), and on a DANGLING `\includegraphics` whose target PDF was NEVER written (the root `.tex` references a figure file that is absent from the bundle / not a valid non-zero PDF per `pdfinfo` — a broken reference that fails to compile and proves no real precompilation happened).
  It is NOT satisfied by an assertion that the externalization step ran, that a figure PDF path exists, or that the plugin emitted a tarball — a bundle whose `.tex` still carries inline tikz, or one whose `\includegraphics` points at a never-written / zero-byte / invalid PDF, would each pass a mere existence/ran check while failing the clauses above; the proof must unpack the REAL tarball and, by INDEPENDENT processes, (a) `pdfinfo` the referenced figure file and confirm it is a valid non-zero PDF, (b) read the root `.tex` and confirm a `\includegraphics` resolves to that on-disk PDF, and (c) grep EVERY `.tex` in the unpacked tree and confirm NO `\begin{tikzpicture}`/`\begin{tikzcd}` source survives.

- **P117 — Every bundled figure is in an arXiv-acceptable format; a non-convertible figure FAILS the export LOUDLY with NO tarball.** Export a witness project whose source REFERENCES an SVG (or other non-arXiv) figure to an arXiv bundle (Phase G / G4, [[phase-g-arxiv-export.md]] "### G4"). The arXiv target is the SAME vendored `arxiv-export` FIREWALL plugin as P114/P115/P116 — its `export.sh` orchestrating script (`src-tauri/resources/vendor/plugins/arxiv-export/export.sh`), extended: AFTER G3's tikz-externalization, the plugin ENUMERATES every figure REFERENCED by the emitted `.tex`, checks each figure's REAL format via `file`/magic bytes (NOT the filename extension), and for any figure that is NOT already PDF/PNG/JPG (the pdfLaTeX-acceptable formats — arXiv does NO on-the-fly conversion and this pipeline is SVG/tikz-centric) it CONVERTS an SVG to PDF using `cairosvg` run through the approved `uvx` runner pathway (`uvx --from cairosvg cairosvg <fig>.svg -o <fig>.pdf` — the same no-sudo runner doctrine as the G5 cleaner; `inkscape`/`rsvg-convert` are NOT installed and `cairosvg` is NOT owned), then REWRITES the `.tex` reference to the converted file.
  The plugin OWNS NO image converter — only the orchestration that selects the binary and the loud-fail gate that validates arXiv-acceptance.
  If a referenced figure CANNOT be made compliant, the plugin FAILS LOUDLY: the export exits NON-ZERO, the OFFENDING filename is NAMED in the export log, and NO tarball is produced — it NEVER silently passes through a figure arXiv would reject.
  The whole md→tex→flatten→materialize→tikz-externalize→figure-format-gate→latexmk-bake-`.bbl`→tar pipeline IS the plugin's script; the app core is unchanged (the same export firewall `run_plugin`/`configure_plugin` in `plugins.rs`). The externally-observable state (compliant case): an INDEPENDENT process unpacking the emitted tarball ENUMERATES every figure REFERENCED by the bundle's `.tex` and, for each, reads the on-disk figure file and confirms via `file`/magic bytes (NOT extension) that it is PDF, PNG, or JPG; AND confirms NO `.svg` file remains ANYWHERE among the bundled/referenced figures (an independent `find … -name '*.svg'` over the unpacked tree returns empty, and no `\includegraphics` in any bundled `.tex` resolves to an SVG). All assertions are by INDEPENDENT processes (`tar`, `find`, `file`/magic-byte checks, grep of the `.tex` references) on the REAL emitted tarball, never the app's report.
  The externally-observable state (loud-failure case): exporting a project that references a figure which CANNOT be made compliant (a non-convertible figure) produces NO tarball at the chosen `{output}` path, the export process exits NON-ZERO, and the export log NAMES the offending figure filename — observed by the INDEPENDENT process as a missing output artifact plus a non-zero exit plus the offending name in the captured log.
  Admissible because it FAILS on a bundle that shipped an SVG arXiv would not convert (an `.svg` is PRESENT and REFERENCED in the unpacked tree, or a referenced figure is magic-byte SVG/other-non-arXiv rather than PDF/PNG/JPG — exactly the format arXiv rejects with no on-the-fly conversion), and on a SILENT pass-through that emits a tarball DESPITE a non-convertible figure (a tarball exists at `{output}` with a zero exit even though a referenced figure could not be made compliant — the loud-failure clause: a real arXiv-rejecting bundle must NEVER ship as a success).
  It is NOT satisfied by an assertion that the figure-format gate ran, that `cairosvg` is reachable, or that the plugin emitted a tarball — a bundle that still carries a referenced `.svg`, one whose figure is magic-byte non-compliant despite a PDF extension, or a pipeline that emits a tarball with a zero exit when a figure could not be converted, would each pass a mere existence/ran check while failing the clauses above; the proof must, for the compliant case, unpack the REAL tarball and by INDEPENDENT processes (a) enumerate every `.tex`-referenced figure and `file`/magic-byte-confirm each is PDF/PNG/JPG and (b) `find` the whole tree and confirm NO `.svg` survives among the referenced figures; and for the loud-failure case, drive an export with a non-convertible figure and INDEPENDENTLY confirm NO tarball at `{output}`, a NON-ZERO exit, and the offending filename NAMED in the log.

- **P118 — `arxiv_latex_cleaner` stripped comments, draft commands, and unused assets, and the bundle carries NO dot-files; with the cleaner UNAVAILABLE the export FAILS LOUDLY.** Export a witness project whose emitted `.tex` (POST md→tex emission, before cleaning) carries a `% SECRET COMMENT` comment line, a `\todo{fix this}` draft command, and an UNUSED image (a real image file present in the bundle but referenced by NO `\includegraphics` in any `.tex`) to an arXiv bundle (Phase G / G5, [[phase-g-arxiv-export.md]] "### G5"). The arXiv target is the SAME vendored `arxiv-export` FIREWALL plugin as P114/P115/P116/P117 — its `export.sh` orchestrating script (`src-tauri/resources/vendor/plugins/arxiv-export/export.sh`), extended: AFTER G4's figure-format gate and the G2 `.bbl` bake, and BEFORE the final `tar`, the plugin runs the REAL `google-research/arxiv-latex-cleaner` UNMODIFIED via the approved `uvx` runner pathway (`uvx --from arxiv-latex-cleaner arxiv_latex_cleaner <bundle> …` — the same no-sudo runner doctrine as the G4 `cairosvg` converter; `arxiv_latex_cleaner` is NOT installed as a host binary and is NOT owned) over the flattened bundle, so the REAL cleaner strips `%`-comments and `comment`/`iffalse` blocks, DELETES the configured draft commands (`\todo{}` etc. via its `commands_to_delete`), PRUNES unused `.tex`/images, RESIZES images under the px cap, and REMOVES auxiliary/dot-files — producing the cleaned folder "ready to upload."
  The cleaner's config (image px cap, `commands_to_delete`, regex rules) is a SHIPPED asset referenced from the plugin's argv (vendored under `src-tauri/resources/vendor/plugins/arxiv-export/`), NOT app config — the export contract is unchanged.
  The plugin OWNS NO comment-stripper / pruner / resizer / dot-file remover: it only INVOKES the real tool and VALIDATES its output; a hand-rolled cleaner is explicitly REJECTED and never substituted.
  `arxiv_latex_cleaner` is a HARD dependency: if the `uvx` pathway is BLOCKED (the cleaner unreachable), the export FAILS LOUDLY naming the missing dependency — it is NEVER skipped, never faked into a "cleaned" tarball.
  The whole md→tex→flatten→materialize→tikz-externalize→figure-format-gate→latexmk-bake-`.bbl`→cleaner→tar pipeline IS the plugin's script; the app core is unchanged (the same export firewall `run_plugin`/`configure_plugin` in `plugins.rs`). The externally-observable state (cleaned case): an INDEPENDENT process unpacking the emitted tarball finds, by INDEPENDENT processes (`tar`, `find`, grep, byte/string reads) over the unpacked tree and NEVER the app's report: (a) NO `%`-comment line containing the string "SECRET COMMENT" in ANY bundled `.tex` (the comment was stripped); (b) NO `\todo` command remaining in any bundled `.tex` (the configured draft command was deleted); (c) the unused image is ABSENT from the bundle (the pruner removed the figure no `\includegraphics` referenced); and (d) NO hidden/dot-file and NO dot-prefixed directory ANYWHERE in the bundle (an independent `find … -name '.*'` over the unpacked tree returns empty — no `.git`, no `.DS_Store`, no dot-dir survives).
  The externally-observable state (loud-failure case): with `arxiv_latex_cleaner` UNAVAILABLE (the `uvx` pathway BLOCKED so the cleaner is unreachable), exporting the witness produces NO tarball at the chosen `{output}` path, the export process exits NON-ZERO, and the export log NAMES the missing `arxiv_latex_cleaner` dependency — observed by the INDEPENDENT process as a missing output artifact plus a non-zero exit plus the named dependency in the captured log.
  The export is NEVER skipped and NEVER emits a "cleaned" tarball when the cleaner is absent.
  Admissible because it FAILS on a bundle the cleaner NEVER touched (the `% SECRET COMMENT` line, the `\todo{fix this}` command, the unused image, and a dot-directory such as `.git` all SURVIVE in the unpacked tree — exactly the state arXiv rejects / the comment-and-draft leakage the cleaner exists to remove), and on a FAIL-OPEN that emits a "cleaned" tarball when the cleaner is ABSENT (a tarball exists at `{output}` with a zero exit even though `arxiv_latex_cleaner` was unreachable — the loud-dependency clause: the export must NEVER ship a fake-cleaned bundle and must NEVER silently skip the hard-dependency cleaner).
  It is NOT satisfied by an assertion that the cleaner step ran, that `arxiv_latex_cleaner` is reachable, that a hand-rolled stripper removed the comment, or that the plugin emitted a tarball — a bundle whose `.tex` still carries the SECRET COMMENT / `\todo` / unused image / a dot-dir, or a pipeline that emits a tarball with a zero exit when the cleaner is unavailable, would each pass a mere existence/ran check while failing the clauses above; the proof must, for the cleaned case, unpack the REAL tarball and by INDEPENDENT processes confirm (a) NO "SECRET COMMENT" comment line, (b) NO `\todo` command, (c) the unused image ABSENT, and (d) NO dot-file / dot-prefixed directory survives; and for the loud-failure case, drive an export with the `uvx` cleaner pathway BLOCKED and INDEPENDENTLY confirm NO tarball at `{output}`, a NON-ZERO exit, and the missing `arxiv_latex_cleaner` dependency NAMED in the log.

- **P119 — End-to-end arXiv-ready tarball integrity: the ONE real bundle satisfies EVERY arXiv hard requirement, and an over-size bundle FAILS LOUDLY with NO tarball.** Export the witness project to an arXiv bundle at a chosen `.tar.gz` path (Phase G / G6 — the CAPSTONE, [[phase-g-arxiv-export.md]] "### G6"). The arXiv target is the SAME vendored `arxiv-export` FIREWALL plugin as P114/P115/P116/P117/P118 — its `export.sh` orchestrating script (`src-tauri/resources/vendor/plugins/arxiv-export/export.sh`) running the FULL pipeline (md→tex→flatten→materialize→tikz-externalize→figure-format-gate→latexmk-bake-`.bbl`→delete-`.bib`→cleaner→dot-file-sweep→`tar`), extended with a FINAL 50 MB SIZE-CAP check: the plugin packages the cleaned bundle with the REAL `tar`/gzip binary (`tar -czf {output}` — own NO archiver, HARD RULE #0), and gates on the bundle's total UNCOMPRESSED size measured by an independent `du`; if the bundle exceeds arXiv's 50 MB cap the export FAILS LOUDLY (exits NON-ZERO, the measured size NAMED in the export log) and NO tarball is shipped to `{output}`. The whole pipeline IS the plugin's script; the app core is unchanged (the same export firewall `run_plugin`/`configure_plugin` in `plugins.rs`). The externally-observable state (ready case): an INDEPENDENT process asserts, on the ONE real emitted tarball, ALL of the following by INDEPENDENT processes only (`tar`, `find`, `file`/magic bytes, `pdfinfo`, `pdftotext`, `du`, byte/string reads) and NEVER the app's report: (a) the tarball EXISTS at EXACTLY the chosen `{output}` path and is a VALID gzip tar (`tar -tzf` lists its members without error / `file` reports gzip-compressed tar); (b) the main `.tex` is at the ROOT of the archive (a top-level member, not nested under a subdirectory); (c) a `<main>.bbl` whose BASENAME EQUALS the main `.tex` basename is present, AND NO `.bib` file exists ANYWHERE in the unpacked tree (P115 — `find … -name '*.bib'` returns empty); (d) NO auxiliary files — NO `*.aux`, `*.log`, `*.out`, `*.dvi` ANYWHERE in the unpacked tree (an independent `find` over each extension returns empty); (e) NO hidden/dot-files and NO dot-prefixed directories ANYWHERE in the unpacked tree (`find … -name '.*'` returns empty — no `.git`, no `.DS_Store`, no dot-dir; P118); (f) EVERY figure REFERENCED by the bundle's `.tex` is PDF, PNG, or JPG verified by `file`/magic bytes (NOT extension), with NO `.svg` among the referenced/bundled figures (P117); (g) the TOTAL UNCOMPRESSED size of the bundle is UNDER 50 MB (measured by an independent `du` over the unpacked tree); (h) unpacking the tarball into a fresh directory with NO access to the user's texmf/system style files (compile with `TEXMFHOME` pointed at an EMPTY dir) and running `latexmk`/`pdflatex` from the bundle ROOT yields a VALID PDF (valid per `pdfinfo`) whose extracted text (`pdftotext`) contains the witnesses "Geometry of Numbers" AND "Minkowski bound" (P114 — the self-contained compile).
  The externally-observable state (over-size loud-failure case): exporting a witness whose bundle EXCEEDS 50 MB produces NO tarball at the chosen `{output}` path, the export process exits NON-ZERO, and the export log NAMES the measured over-cap size — observed by the INDEPENDENT process as a missing output artifact plus a non-zero exit plus the size named in the captured log.
  The export NEVER ships an over-size tarball.
  Admissible because it FAILS on ANY single arXiv hard-requirement violation in the REAL emitted artifact — a `.bib` PRESENT (the citation never baked, clause c), an `.aux`/`.log`/`.out`/`.dvi` LEFT IN (auxiliaries not removed, clause d), a `.git`/dot-dir SHIPPED (clause e), an SVG figure REFERENCED (clause f), a NON-self-contained bundle that errors under the empty-`TEXMFHOME` ROOT compile (clauses b/h — dangling `\input` or unmaterialized macro), a main `.tex` NOT at the root (clause b), or an OVER-SIZE bundle that ships anyway (clause g + the loud-failure clause) — none of which a smoke "the export ran" / "a tarball exists" check would catch.
  It is NOT satisfied by an assertion that the `arxiv-export` plugin ran, that the `tar` step executed, or that a tarball merely EXISTS at `{output}` — a tarball that carries a `.bib`, leaves an `.aux`, ships a `.git` dir, references an `.svg`, fails the no-system-styles ROOT compile, or exceeds 50 MB yet ships, would each pass a mere existence/ran check while failing the clauses above; the proof must inspect the ONE real emitted tarball by INDEPENDENT processes for EVERY clause (a)–(h) simultaneously on that single artifact, AND independently drive an over-size export and confirm NO tarball at `{output}`, a NON-ZERO exit, and the measured over-cap size NAMED in the log.
  This capstone SUBSUMES the per-step properties (P114 self-contained compile, P115 `.bbl`/no-`.bib`, P117 figure formats, P118 dot-files) but asserts them ALL on the ONE real upload-ready tarball, plus the auxiliary-file and 50 MB-cap arXiv requirements the per-step proofs do not.

### Milestone — Phase H: low-priority QOL / writing comfort (P120–P124 reserved block; P120, P121, P122, P123 ratified; P124 reserved spare, now unused — readability folded into P120)

P121 (added 2026-06-20) covers the Phase H sub-milestone H.2 "three-way edit/preview/split view-mode toggle", authored from [[phase-h-qol.md]] (the H.2 section + the drafted P121) and ratified by the 2026-06-20 controller decision ("execute all phases, no stops"; obligations P120–P123 as drafted, ceiling P119 from Phase G, P124 reserved spare; Phase H specs continue p128+). It is LAYOUT STATE over infrastructure that already ships, NOT a new layout engine: the editor|preview split is a horizontal `dockview` `SplitviewComponent` created by `createSplitLayout` (`src/lib/dockview.ts` — `addPanel` editor/preview, each exposing a `data-pane` element the Svelte editor/preview wrappers portal into).
H.2 adds a `setView(mode: 'editor'|'preview'|'split')` to the returned `SplitLayout` that toggles the editor/preview panel visibility via the existing `SplitviewComponent` per-view visibility API and re-`layout()`s, returning the SAME `editorPane`/`previewPane` elements so the portal mounts stay attached — it NEVER tears down and rebuilds the splitview (a rebuild would lose the P13/P15 editor:preview ratio and the portal targets).
The view mode is config-owned (`config.rs::Editor` — a `view_mode` enum `editor`/`preview`/`split`, `deny_unknown_fields`-validated in `validate()`, round-tripped by `save_config`, the P9 invariant — mirrored in `src/lib/types.ts`), restored at launch; an unknown `view_mode` is a LOUD config error, never silently coerced to a default (single-user pre-launch Linux dev software: no fallbacks, no soft defaults).
`App.svelte` owns a `viewMode` $state driving `split.setView`, command-palette entries `view:editor`/`view:preview`/`view:split`, and a `setViewMode(mode)`/`viewMode()` pair on the `__PPE_E2E__` hook.
It REUSES the pane-width measurement `tests/proof/support/layout.ts` that P13/P15 use, so the ratio-preservation clause is measured on the SAME real surface, and the hermetic-`XDG_CONFIG_HOME` persist/relaunch round-trip the P9 class proves.
It states the OBSERVABLE behaviour ONLY — no `setView` visibility-toggle, re-`layout()`, config-enum, or palette-wiring internals (the wiring belongs to the implementer, not this obligation).
It is proven through the real app on a real display via `tauri-plugin-playwright`, with the pane widths measured independently of the app's own report.
It is admissible only if it FAILS on a plausibly broken app.
P120 (comfort modes) is ratified below; P122 (batch export) is ratified below; H.6 (autocorrect/magic quotes) is DEPRIORITIZED and is deliberately NOT allocated an obligation — a correctness hazard (smart-quote substitution corrupting `"` inside `\text{}` / `$…$` LaTeX-bearing source), recorded so its absence is not mistaken for an oversight.

P120 (added 2026-06-20) covers the Phase H sub-milestone H.1 "distraction-free / typewriter / readability modes", authored from [[phase-h-qol.md]] (the H.1 section + the drafted P120 at "## Proposed proof obligations") and ratified by the 2026-06-20 controller decision ("execute all phases, no stops"; obligations P120–P123 as drafted, ceiling P119 from Phase G, P124 reserved spare; Phase H specs continue p128+).
It is THREE INDEPENDENT EDITOR-PRESENTATION comfort modes — each refining surfaces that already ship via the established `Compartment` + post-mount-reconfigure machinery (`spellCompartment`, `src/lib/components/EditorPane.svelte`) and an App-shell CSS state, NOT a new editor engine: (a) TYPEWRITER keeps the caret line vertically centered in the editor viewport (the published CM6 typewriter recipe — `EditorView.scrollMargins` / scroll-into-view centering — in a `typewriterCompartment` next to `spellCompartment`, reconfigured post-mount from config); (b) DISTRACTION-FREE hides the sidebar / insertion bar / status bar (an App-shell CSS state driven from `src/App.svelte` — a `viewComfort` $state toggling a CSS class that hides `ActivityBar`/`InsertionBar`/`StatusBar` — NOT editor infra); (c) READABILITY adds a sentence-level CM6 `Decoration` coloring layer (a thin decoration over sentence spans in a `readabilityCompartment`, NOT a new engine, respecting the SAME math/code exclusion predicate the fork exposes that Phase A/B share).
RESEARCH-FIRST (HARD RULE #0): all three lean on PUBLISHED CM6 extensions (typewriter scroll-margin, readability decorations) + the EXISTING Compartment/reconfigure machinery — refine, do NOT greenfield an editor engine; the published CM6 example(s) leveraged are named in the RED commit.
The three toggles are config-owned (`config.rs::Editor` — a `[editor.comfort]` sub-table of booleans `{distraction_free, typewriter, readability}`, validated in `validate()`, round-tripped by `save_config`, the P9 invariant — mirrored in `src/lib/types.ts`), every mode OFF by default, restored at launch; the `App.svelte` command palette carries `comfort:distraction-free` / `comfort:typewriter` / `comfort:readability` entries toggling each.
READABILITY is FOLDED into P120 (promoted from the reserved P124 spare) so the third BUILT mode is PROVEN, not dead code; with readability folded in, P124 remains the reserved spare but is now UNUSED.
CRITICAL CONFIG TRAP (the F4 + H.2 lesson): the new `[editor.comfort]` sub-table is REQUIRED config (NO `#[serde(default)]`, `deny_unknown_fields`) — single-user pre-launch Linux dev software, no fallbacks, no soft defaults, fail loud — so it MUST be emitted by BOTH `scripts/first-run.sh` (the generated config heredoc) AND `scripts/provision-proof.sh`, defaulted to ALL FALSE (every mode OFF), or the config-load specs d14/p10/p09 break; emitting the new required field in `config.rs` WITHOUT emitting it in those two scripts is the exact regression that broke d14/p10 twice this milestone.
The comfort defaults are OFF, so P120 does NOT weaken P13/P14/P15 — those chrome-measurement specs run UNCHANGED (the distraction-free chrome is present until the mode is explicitly enabled).
It states the OBSERVABLE behaviour ONLY — no `Compartment`-reconfigure, scroll-margin, decoration-layer, CSS-class, config-subtable, or palette-wiring internals (the wiring belongs to the implementer, not this obligation).
It is proven through the real app on a real display via `tauri-plugin-playwright`, with the caret-line viewport offset, the chrome DOM presence/visibility, and the sentence-decoration marks read from the live editor/shell DOM, and the comfort toggles driven from a hermetic `XDG_CONFIG_HOME`, independently of the app's own report.
It is admissible only if it FAILS on a plausibly broken app.

P123 (added 2026-06-20) covers the Phase H sub-milestone H.4 "reading-time metric in the status cluster", authored from [[phase-h-qol.md]] (the H.4 section + the drafted P123) and ratified by the 2026-06-20 controller decision (Phase H obligations P120–P123 as drafted, ceiling P119 from Phase G, P124 reserved spare).
It is a TRIVIAL DERIVATION in the status cluster that already ships, NOT a new buffer scan and NOT new App state: the `wordCount` is already `$state` recomputed on every edit (`src/App.svelte` — `content.split(/\s+/).filter(Boolean).length`) and threaded into the status cluster, which renders `{wordCount} words` (`src/lib/components/StatusBar.svelte`). H.4 adds a `readingWpm` prop to `StatusBar` and a `$derived` `readingMinutes = Math.ceil(wordCount / readingWpm)` rendered as a SIBLING `<span>` to the word count — it reuses the existing `wordCount` (no new scan, no new state beyond threading the WPM through).
The words-per-minute is config-owned (`config.rs::Editor` — a `reading_wpm: u32`, range-validated in `validate()` exactly like `font_size`/`debounce_ms` against a MIN/MAX OSOT, so an out-of-range value is a LOUD config error; round-tripped by `save_config`, the P9 invariant — mirrored in `src/lib/types.ts`), never a hardcoded constant (single-user pre-launch Linux dev software: no fallbacks, no soft defaults).
It states the OBSERVABLE behaviour ONLY — no `$derived`-`ceil`, prop-threading, or config-field wiring internals (the wiring belongs to the implementer, not this obligation).
It is proven through the real app on a real display via `tauri-plugin-playwright`, with the displayed reading time read from the live status-bar DOM and the config WPM driven from a hermetic `XDG_CONFIG_HOME`, independently of the app's own report.
It is admissible only if it FAILS on a plausibly broken app.

P122 (added 2026-06-20) covers the Phase H sub-milestone H.3 "batch / multi-format export in one action", authored from [[phase-h-qol.md]] (the H.3 section + the drafted P122) and ratified by the 2026-06-20 controller decision (Phase H obligations P120–P123 as drafted, ceiling P119 from Phase G, P124 reserved spare).
It is a LOOP over the export path that already ships, NOT a new export mechanism: export was migrated to the plugin FIREWALL (P66) — the export targets are the DISCOVERED `category = "export"` plugins (`pandoc-html-export`, `pandoc-pdf-export`, the Phase-G `arxiv-export`, …), each declaring its own output `extension`, run through the generic firewall by `runPluginToPath`/`exportViaPlugin` (`src/App.svelte` — `api.runPlugin` → `run_plugin` in `src-tauri/src/plugins.rs`), NOT an app-core `[export.<id>]` config table (that table is retired).
H.3 COMPOSES that EXISTING single per-plugin export path (the `batchConvert` shape) — it iterates the discovered export-category plugins and calls the SAME `runPluginToPath(plugin.id, target)` per plugin (each `target` the chosen base name + that plugin's declared `extension`), adding NO new export path and leaving the per-plugin export + its plugin validation UNTOUCHED. Every per-target run rides the SAME save-gate (P47) the single export uses — an identity-less buffer resolves a durable destination FIRST, never a silent partial run — and a FAILED target export is LOUD (a failed target is not a silent partial: the batch surfaces it, it does not quietly write fewer files), single-user pre-launch Linux dev software, no fallbacks, no soft defaults.
`App.svelte` owns the batch action (a command-palette `export:all`-class entry and an `exportAll(baseDir)`-class hook on the `__PPE_E2E__` object — the sibling of the per-plugin `exportViaPluginById`/`runPlugin` hooks), each per-target run going through the unchanged per-plugin firewall path.
It states the OBSERVABLE behaviour ONLY — no loop, hook-wiring, or palette-entry internals (the wiring belongs to the implementer, not this obligation).
It does NOT weaken P7/P8/P12/P66 — the per-plugin export specs run UNCHANGED; P122 proves the LOOP over them writes N real files in one action.
It is proven through the real app on a real display via `tauri-plugin-playwright`, with the real configured export-category plugin commands (real pandoc / lualatex per plugin) producing the artifacts and an INDEPENDENT process reading the chosen directory and the on-disk bytes, independently of the app's own report.
A batch with a PDF target runs `lualatex` (a hard dependency, the P8 engine), so the proof allows generous waits; it is admissible only if it FAILS on a plausibly broken app.

- **P120 — Comfort modes change the editor presentation and round-trip through config.** Three independent editor-presentation comfort modes, each enabled via its command-palette toggle / `__PPE_E2E__` hook, observed on REAL editor/shell DOM (no mode satisfied by a symbol/command merely existing):
  (1) TYPEWRITER — enabling it MOVES the caret line toward the VERTICAL CENTER of the editor viewport: the caret line's offset from the viewport top falls within a CENTERED band (e.g. within a few px of half the editor-viewport height), MEASURABLY different from its un-centered offset with the mode OFF (the same buffer + caret position, toggled on vs off, yields a centered vs non-centered caret-line offset).
  (2) DISTRACTION-FREE — enabling it REMOVES/HIDES the sidebar + insertion bar + status bar (observed via their DOM ABSENCE or zero-visibility — the `ActivityBar`/`InsertionBar`/`StatusBar` elements are gone / not laid out), and DISABLING it RESTORES all three (they reappear / are laid out again).
  (3) READABILITY — enabling it ADDS VISIBLE sentence-level decoration marks in the editor DOM (the sentence-decoration spans/marks are PRESENT when the mode is ON and ABSENT when it is OFF — folded from the reserved P124 spare so the third built mode is PROVEN, not dead).
  (4) ROUND-TRIP — the enabled modes, after a save + relaunch under a hermetic `XDG_CONFIG_HOME`, are STILL active (typewriter still centers, distraction-free still hides the chrome, readability still marks — the `[editor.comfort]` booleans round-tripped through the on-disk `config.toml`, the P9 class).
  Admissible because it FAILS on: NO comfort extensions (typewriter does NOT center the caret line — the offset is unchanged from the mode-off position; distraction-free LEAVES the sidebar/insertion-bar/status-bar visible; readability adds NO sentence marks to the editor DOM); and a NON-PERSISTED toggle (the modes RESET after relaunch — the caret recenters off, the chrome returns, the marks vanish — because the `[editor.comfort]` sub-table did not round-trip, the P9 class).
  It is NOT satisfied by an assertion that a `typewriterCompartment`/`readabilityCompartment`/`viewComfort` symbol or a `comfort:typewriter`/`comfort:distraction-free`/`comfort:readability` command merely EXISTS — a typewriter that is wired but never centers the caret line (the measured offset is identical mode-on vs mode-off), a distraction-free that leaves the chrome laid out, a readability that adds no marks, or any mode that does not survive relaunch would each pass an existence check while failing every clause above; the proof must (a) measure the caret-line viewport offset toggling typewriter on vs off, (b) read the shell DOM for the `ActivityBar`/`InsertionBar`/`StatusBar` absence under distraction-free and their restoration on disable, (c) read the editor DOM for the presence/absence of the sentence-decoration marks toggling readability, and (d) confirm all three survive a save + relaunch under a hermetic `XDG_CONFIG_HOME` — all on the live DOM, independently of the app's own report.
  It does NOT weaken P13/P14/P15 — the comfort modes default OFF (the `[editor.comfort]` booleans are all `false` in the generated config, emitted by BOTH `scripts/first-run.sh` and `scripts/provision-proof.sh`), so the distraction-free chrome is PRESENT and those chrome-measurement specs run UNCHANGED.

- **P122 — Batch export writes one real, non-empty file per configured target in a single action.** With TWO OR MORE export-category plugins configured (e.g. an html plugin and a pdf plugin), invoking the SINGLE batch-export action writes a REAL, NON-EMPTY output file for EVERY configured target into the chosen directory — each file named with that plugin's declared `extension`, each produced by that plugin's REAL command (real pandoc / lualatex per plugin), verified by an INDEPENDENT process reading the directory and finding exactly N files (N = the number of configured export-category plugins) whose contents are the real export bytes, NOT zero-length.
  The directory listing and the file bytes are read by an independent process, not from the app's own report.
  Admissible because it fails on: NO batch action (only ONE or ZERO files are written — a single export, or nothing, ran; the one-action-N-files clause fails); a batch that SKIPS targets (FEWER than N files appear in the directory — the loop did not cover every configured export-category plugin); and an EMPTY-WRITE (a file with the right name and `extension` EXISTS but holds NO export bytes / is zero-length — the plugin's real command did NOT actually run for that target, only the file was touched).
  It is NOT satisfied by an assertion that an `exportAll`/`export:all` symbol or a batch command merely EXISTS — a batch that is wired but writes only one file, one that writes N empty (zero-length) files, or one that writes fewer than N files would each pass an existence check while failing every clause above; the proof must read the chosen directory with an independent process and confirm EXACTLY N files (one per configured export-category plugin), each NAMED with its plugin's declared `extension`, each NON-EMPTY and carrying the REAL export bytes that plugin's command produces.
  It does NOT weaken P7/P8/P12/P66 — those per-plugin export specs run UNCHANGED; P122 proves only the LOOP over the existing per-plugin firewall path.

- **P121 — Three-way view toggle hides a pane and preserves the split ratio on return.** Setting the view mode to `editor` HIDES the preview pane — observed: the `data-pane="preview"` element has ZERO measured width / is not laid out — while the editor occupies the FULL width; setting it to `preview` HIDES the editor (the `data-pane="editor"` element has zero measured width) while the preview occupies the full width; and setting it back to `split` RESTORES BOTH panes at the SAME ratio they held before the toggle (the editor:preview width ratio measured before, e.g. via a prior splitter drag to a non-50/50 position, recurs within a few px), and the chosen mode PERSISTS across a save + relaunch under a hermetic `XDG_CONFIG_HOME` (the config round-trip — `view_mode` parses back from the on-disk `config.toml` and the relaunched app restores that mode).
  The pane widths are measured by the `tests/proof/support/layout.ts` measurement P13/P15 use, independently of the app's own report.
  Admissible because it fails on: NO view toggle (both panes always show — setting `editor`/`preview` has NO effect on the measured pane widths, so neither pane is ever hidden); a REBUILD-based toggle that loses the ratio (returning to `split` yields a reset 50/50 instead of the prior non-50/50 ratio — the P15 regression a naive teardown/rebuild of the splitview causes, because the surviving split's proportions are not preserved across the round-trip); and a NON-PERSISTED mode (the selected `view_mode` resets after relaunch because the config did not round-trip — the P9 class, the on-disk `config.toml` does not carry the chosen mode or the relaunched app ignores it).
  It is NOT satisfied by an assertion that a `setView`/`viewMode` symbol or a `view:editor`/`view:preview`/`view:split` command merely EXISTS — a toggle that is wired but never hides a pane (both `data-pane` widths stay non-zero in every mode), one that hides a pane by rebuilding the splitview and returns to a reset 50/50 instead of the prior ratio, or one whose mode does not survive relaunch would each pass an existence check while failing every clause above; the proof must measure (via `tests/proof/support/layout.ts`) the `data-pane="preview"` width going to ZERO under `editor` and the `data-pane="editor"` width going to ZERO under `preview`, the prior non-50/50 ratio RECURRING on the return to `split`, and the chosen mode SURVIVING a save + relaunch under a hermetic `XDG_CONFIG_HOME`.

- **P123 — Reading-time metric reflects the word count and the config WPM.** With a buffer of a KNOWN word count and a config-owned `reading_wpm` (set under a hermetic `XDG_CONFIG_HOME`), the status cluster shows a reading-time value EQUAL to `ceil(words / wpm)` — read from the live status-bar DOM (the sibling `<span>` next to `{wordCount} words`), independently of the app's own report.
  EDITING the buffer to change the word count UPDATES the displayed reading time (a buffer edited from W₁ words to a different W₂ words makes the displayed reading time move from `ceil(W₁ / wpm)` to `ceil(W₂ / wpm)`). Pointing config at a DIFFERENT `reading_wpm` makes the SAME buffer show a DIFFERENT reading time (the displayed value tracks the config WPM, not a constant).
  Admissible because it fails on: NO reading-time metric (the status cluster shows ONLY the word count — no sibling span carrying a reading-time value); a HARDCODED WPM (changing the config `reading_wpm` does NOT change the displayed reading time for the same buffer — the config is dead, the divisor is a literal); and a STALE metric (editing the buffer to change the word count does NOT update the displayed reading time — the value is computed once and never re-derived from the live `wordCount`). It is NOT satisfied by an assertion that a reading-time span or a `readingMinutes`/`readingWpm` symbol merely EXISTS — a span that is rendered but shows a fixed value regardless of word count, one whose divisor is a hardcoded literal so a different config `reading_wpm` yields the same displayed time, or one that does not re-derive when the buffer is edited would each pass an existence check while failing every clause above; the proof must read the displayed reading time from the status-bar DOM and confirm it EQUALS `ceil(words / wpm)` for a known word count, CHANGES when the buffer's word count changes, and DIFFERS for the same buffer when config carries a different `reading_wpm`.

- **P128 — a tikz FILE renders standalone as an inline vector figure in the live preview, via the user-owned template, and a style the user defines governs the render.** (added 2026-06-20; the issue-#2 realignment replacement for P100, carrying the burden retired off P91/P92.) Opening a tikz FILE (a `.tikz` file whose content is a `tikzpicture`/`tikzcd` body) in the app renders it in the live preview as a real inline VECTOR figure (an `<svg>` carrying drawing content — `path`/`line`/`g`/`text`), and the raw tikz source is NOT shown verbatim — proving the file is COMPILED, not echoed, through the file-mode render path (the `tikz-renderer` plugin wrapping the source in the user-owned `standalone-tikz.tex` template, pdflatex → pdf2svg). The template OWNS its preamble via `\usepackage{dzg-tikz}` over the user's styles tree: a named tikz style/macro the user defines ONLY in that styles tree visibly determines the rendered figure (and changing that definition changes the render), with NO app-injected palette, NO `[figures]` config table, and NO `shared.*` asset.
  The preview SVG is read from the preview DOM by the harness, independently of the app's own report.
  Admissible because it fails on: a tikz file that renders RAW SOURCE (no `<svg>` with drawing content appears — the file was not compiled); a render that IGNORES the user's styles tree (a figure using a user-defined style does not reflect it, or changing the style in the styles tree does not change the render — proving an injected/hardcoded palette rather than the template-owned preamble); and a tikz file routed through the MARKDOWN renderer (the source is rendered as a code block / passed through, not compiled to a figure). It is NOT satisfied by an assertion that a `render_tikz`/`tikz-renderer`/`isTikzFile` symbol merely EXISTS — the proof must open a real `.tikz` file on a real display and confirm the compiled inline SVG appears, the raw source does not, and a user-styles-tree change moves the render.
  It folds the old `tikz-toolchain` doctor gate: the `tikz-renderer` plugin's doctor check fails loud if pdflatex/pdf2svg or the user-owned template is absent.

### Render-matrix realignment (2026-06-20, issue #2 → systemic)

The tikz drift was the tip of a pattern: a *render target* (input type × output mode +
template + engine) was reified into bespoke machinery, hardcoded into the core, or left
half-wired. The ratified model: the app owns only the input data, the current output mode
(the expected data contract — html bytes for the live preview, a file for export), and
GUI exposure; **which renderer/export plugin and which template fill a cell is discovered
+ user-selected**, never hardcoded. Plugins declare `inputs` in their manifest; the app
builds the (open file type → candidate render targets) matrix from discovery.

Status of the realignment obligations:

- **BUILT (WS1/WS2, verified: build + typecheck + hermetic --doctor).** Plugins declare
  `inputs`; one `render(renderer_id, template, …)` IPC replaces the per-mode commands;
  the frontend resolves the renderer by file type from discovery (default `[renderer].active`);
  the hardcoded `SLIDES_RENDERER_ID`/`TIKZ_RENDERER_ID` consts and `isTikzFile`/`slidesMode`
  booleans are gone; a `{template}` placeholder is forwarded to renderers.

- **P129 — render-target selector (PENDING, GUI-proof-gated).** With a markdown file open,
  the user can select among the discovered render targets compatible with the current
  input + output (e.g. Article preview vs Slides for html; Article PDF vs Beamer PDF for
  export); selecting a target re-renders through it (the selected renderer + template,
  forwarded via `{template}`), and the choice persists across relaunch (config round-trip,
  the `view_mode` precedent). Admissible only if it fails on: no selector; a selector that
  does not change the rendered output; a non-persisted choice.

- **P130 — slides as a template selection (PENDING, GUI-proof-gated).** Selecting the
  Slides target renders the SAME markdown as a reveal.js DECK in the preview iframe (the
  shipped `revealjs-renderer` plugin = `pandoc --to revealjs` + the `pandoc_revealjs_template.html`
  template); the Beamer PDF target exports the same source to a beamer PDF via the pdf
  export plugin + `beamer_template.latex`. Slides is NOT a bespoke mode — it is a
  discovered render target. (Replaces the deleted dead `slidesMode`/`revealjs-renderer`-const
  wiring; ship the vendored `revealjs-renderer` plugin, doctor-clean like `tikz-renderer`.)

- **DEFERRED to a follow-up (recorded so not silently dropped).**
  - **P131 — `.tex` render cell**: opening a `.tex` file renders it (tex→html and/or
    tex→pdf) via a latex renderer/export target + template, instead of falling through to
    the markdown renderer (garbage today).
  - **P132 — `.bib` handling**: a bibtex editor grammar (the editor forces the latex
    grammar on every file today) and a decision on whether `.bib` is a render target
    (formatted bibliography preview) vs citation-data-only.
  - **Per-file-type editor language** selection (md/tex/tikz → latex grammar; bib → bibtex)
    folded into the above.

- **Carried tikz debt** (from the Phase D realignment note above): **P128** (file-mode
  tikz render) + the **P93/P95/P98** file-mode reworks + retiring the injection-only
  `p100/p101/p102/p105` specs — all GUI-proof-gated, driven red→green with the suite.

## Verification vehicle

Real app on a real display via `tauri-plugin-playwright` (precedent: the sibling repo `~/gitclones/pandoc-preview-greenfield` proof harness; see also the `tauri-playwright` skill).
Hermetic per-run temp project dirs and `XDG_CONFIG_HOME`; disk assertions via independent processes; pandoc, lualatex, gum as hard dependencies.
No mocks, no skips, no forced error modes.

P10 caveat: the only obligation requiring PTY automation of gum; if brittle, record as explicit proof debt — never weaken to existence checks.

P90 caveat: the one obligation NOT driven through the playwright/display vehicle — the tikz-subset parser is owned pure-Rust app code with no webview surface, so P90 is proven by `cargo test` over real `.tikz` fixtures (no display, no mocks).
It is still held to the round-trip-equality bar above — never weakened to an existence check on a parse function.
