% Default preview bibliography. install-assets.sh symlinks this into
% ~/.pandoc/bib/references.bib so the preview command's --citeproc/--bibliography
% always resolve against a real file. Replace it with your own real file at that
% path and install-assets preserves your override (it never clobbers a real file).
%
% This starter is intentionally empty of entries: a document with no citations
% renders unchanged, and a document that cites a key not present here renders the
% key with a citeproc "reference not found" warning (loud, not silent).
