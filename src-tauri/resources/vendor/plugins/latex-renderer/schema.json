{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "latex-renderer plugin config",
  "description": "The latex renderer is self-contained: it owns its pandoc --from latex invocation and resolves the html template itself. It requires no config — an absent [plugin.latex-renderer] section validates as the empty object.",
  "type": "object",
  "additionalProperties": false,
  "properties": {}
}
