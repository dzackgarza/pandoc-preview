{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "latex-pdf-export plugin config",
  "description": "Self-contained: it compiles the .tex with latexmk -lualatex. No config — an absent [plugin.latex-pdf-export] section validates as the empty object.",
  "type": "object",
  "additionalProperties": false,
  "properties": {}
}
