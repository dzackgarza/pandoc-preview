{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "revealjs-renderer plugin config",
  "description": "The reveal.js renderer is self-contained: it owns its pandoc --to revealjs invocation and resolves the selected template itself. It requires no config — an absent [plugin.revealjs-renderer] section validates as the empty object.",
  "type": "object",
  "additionalProperties": false,
  "properties": {}
}
