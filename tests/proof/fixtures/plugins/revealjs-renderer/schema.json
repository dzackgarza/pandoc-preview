{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "revealjs-renderer config section",
  "type": "object",
  "additionalProperties": false,
  "required": ["command"],
  "properties": {
    "command": {
      "type": "string",
      "description": "The raw pandoc command STRING that emits a reveal.js deck (canonical), e.g. 'pandoc --from markdown --to revealjs --standalone --embed-resources'. render.sh shlex-tokenizes and runs it verbatim, layering the volatile render context."
    }
  }
}
